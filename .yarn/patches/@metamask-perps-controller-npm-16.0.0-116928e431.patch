diff --git a/dist/providers/HyperLiquidProvider.cjs b/dist/providers/HyperLiquidProvider.cjs
index 2609f4fda857c26dede61b0236f1d9def77bb897..c9b0a13132e78fc342033a02c3d790fc4b17c05f 100644
--- a/dist/providers/HyperLiquidProvider.cjs
+++ b/dist/providers/HyperLiquidProvider.cjs
@@ -2188,7 +2188,7 @@ class HyperLiquidProvider {
             // when available, otherwise query that DEX over HTTP. If neither source
             // answers, fail closed: trusting the snapshot would recreate the stale
             // side/size reduce-only rejection this method is meant to prevent.
-            const currentPositions = await __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_getPositionsForOperation).call(this, (0, hyperLiquidAdapter_js_1.parseAssetName)(symbol).dex ?? '');
+            const currentPositions = await __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_getPositionsForOperation).call(this, (0, hyperLiquidAdapter_js_1.parseAssetName)(symbol).dex ?? '', { revalidateMissingSymbol: symbol });
             const position = currentPositions.find((pos) => pos.symbol === symbol);
             if (livePosition &&
                 position &&
@@ -10258,13 +10258,21 @@ async function _HyperLiquidProvider_getStandaloneValidatedDexs() {
  * position list.
  * @returns Positions from one provenance-safe cache/REST path.
  */
-async function _HyperLiquidProvider_getPositionsForOperation(targetDex) {
+async function _HyperLiquidProvider_getPositionsForOperation(targetDex, options = {}) {
     if (targetDex !== undefined) {
         const targetPositions = __classPrivateFieldGet(this, _HyperLiquidProvider_subscriptionService, "f").getCachedPositionsForDex(targetDex);
         if (targetPositions !== null) {
-            return [...targetPositions];
+            const revalidateMissingSymbol = options.revalidateMissingSymbol;
+            const shouldRevalidate = revalidateMissingSymbol !== undefined &&
+                targetPositions.find((position) => position.symbol === revalidateMissingSymbol) === undefined;
+            if (!shouldRevalidate) {
+                return [...targetPositions];
+            }
+            __classPrivateFieldGet(this, _HyperLiquidProvider_deps, "f").debugLogger.log('Target symbol missing from position cache: revalidating REST positions', { dex: targetDex || 'main', symbol: revalidateMissingSymbol });
+        }
+        else {
+            __classPrivateFieldGet(this, _HyperLiquidProvider_deps, "f").debugLogger.log('Target DEX position cache unavailable: fetching REST positions', { dex: targetDex || 'main' });
         }
-        __classPrivateFieldGet(this, _HyperLiquidProvider_deps, "f").debugLogger.log('Target DEX position cache unavailable: fetching REST positions', { dex: targetDex || 'main' });
         const { answered, positions } = await __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_queryDexPositions).call(this, targetDex || null);
         if (!answered) {
             throw new Error(perpsErrorCodes_js_1.PERPS_ERROR_CODES.PROVIDER_NOT_AVAILABLE);
diff --git a/dist/providers/HyperLiquidProvider.mjs b/dist/providers/HyperLiquidProvider.mjs
index 4219c346871e30b4168539c2a5f1cfcb5cc70ab2..13f6c5fd5641b7459ebc1a7fb264733f6485e3fc 100644
--- a/dist/providers/HyperLiquidProvider.mjs
+++ b/dist/providers/HyperLiquidProvider.mjs
@@ -2185,7 +2185,7 @@ export class HyperLiquidProvider {
             // when available, otherwise query that DEX over HTTP. If neither source
             // answers, fail closed: trusting the snapshot would recreate the stale
             // side/size reduce-only rejection this method is meant to prevent.
-            const currentPositions = await __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_getPositionsForOperation).call(this, parseAssetName(symbol).dex ?? '');
+            const currentPositions = await __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_getPositionsForOperation).call(this, parseAssetName(symbol).dex ?? '', { revalidateMissingSymbol: symbol });
             const position = currentPositions.find((pos) => pos.symbol === symbol);
             if (livePosition &&
                 position &&
@@ -10254,13 +10254,21 @@ async function _HyperLiquidProvider_getStandaloneValidatedDexs() {
  * position list.
  * @returns Positions from one provenance-safe cache/REST path.
  */
-async function _HyperLiquidProvider_getPositionsForOperation(targetDex) {
+async function _HyperLiquidProvider_getPositionsForOperation(targetDex, options = {}) {
     if (targetDex !== undefined) {
         const targetPositions = __classPrivateFieldGet(this, _HyperLiquidProvider_subscriptionService, "f").getCachedPositionsForDex(targetDex);
         if (targetPositions !== null) {
-            return [...targetPositions];
+            const revalidateMissingSymbol = options.revalidateMissingSymbol;
+            const shouldRevalidate = revalidateMissingSymbol !== undefined &&
+                targetPositions.find((position) => position.symbol === revalidateMissingSymbol) === undefined;
+            if (!shouldRevalidate) {
+                return [...targetPositions];
+            }
+            __classPrivateFieldGet(this, _HyperLiquidProvider_deps, "f").debugLogger.log('Target symbol missing from position cache: revalidating REST positions', { dex: targetDex || 'main', symbol: revalidateMissingSymbol });
+        }
+        else {
+            __classPrivateFieldGet(this, _HyperLiquidProvider_deps, "f").debugLogger.log('Target DEX position cache unavailable: fetching REST positions', { dex: targetDex || 'main' });
         }
-        __classPrivateFieldGet(this, _HyperLiquidProvider_deps, "f").debugLogger.log('Target DEX position cache unavailable: fetching REST positions', { dex: targetDex || 'main' });
         const { answered, positions } = await __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_queryDexPositions).call(this, targetDex || null);
         if (!answered) {
             throw new Error(PERPS_ERROR_CODES.PROVIDER_NOT_AVAILABLE);
