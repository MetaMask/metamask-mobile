diff --git a/lib/typescript/src/BrazePlugin.d.ts b/lib/typescript/src/BrazePlugin.d.ts
index cfa21dcbad41d79569279840b208383999106fcd..e6ae83ffd62b417c8e34dfe9b3ebd59b1cc9e26d 100644
--- a/lib/typescript/src/BrazePlugin.d.ts
+++ b/lib/typescript/src/BrazePlugin.d.ts
@@ -4,6 +4,14 @@ export declare class BrazePlugin extends DestinationPlugin {
     key: string;
     private lastSeenTraits;
     private revenueEnabled;
+    private brazeProfileId;
+    /**
+     * Set the Braze profile ID used for `Braze.changeUser()`.
+     * When undefined the plugin becomes a complete no-op: identify, track and
+     * flush calls are silently dropped so no data reaches Braze under the
+     * wrong identity.
+     */
+    setBrazeProfileId(profileId: string | undefined): void;
     update(settings: SegmentAPISettings, _: UpdateType): void;
     /**
      * Cleans up the attributes to only send valid values to Braze SDK
diff --git a/src/BrazePlugin.tsx b/src/BrazePlugin.tsx
index 3b2df7bbbff4971c5cb894ae8ff3663c5e945b06..ee9da514a20c959ff9adf1bacb6aa2fb699bc31d 100644
--- a/src/BrazePlugin.tsx
+++ b/src/BrazePlugin.tsx
@@ -38,6 +38,25 @@ export class BrazePlugin extends DestinationPlugin {
   key = 'Appboy';
   private lastSeenTraits: UserInfoState | undefined;
   private revenueEnabled = false;
+  private brazeProfileId: string | undefined;
+
+  /**
+   * Set the Braze profile ID used for `Braze.changeUser()`.
+   *
+   * When a profileId is provided, `Braze.changeUser()` is called immediately
+   * so subsequent track calls are attributed to the correct user even before
+   * the next Segment identify event arrives.
+   *
+   * When undefined the plugin becomes a complete no-op: identify, track and
+   * flush calls are silently dropped so no data reaches Braze under the
+   * wrong identity.
+   */
+  setBrazeProfileId(profileId: string | undefined) {
+    this.brazeProfileId = profileId;
+    if (profileId !== undefined) {
+      Braze.changeUser(profileId);
+    }
+  }
 
   update(settings: SegmentAPISettings, _: UpdateType) {
     const brazeSettings = settings.integrations[
@@ -85,6 +104,10 @@ export class BrazePlugin extends DestinationPlugin {
   };
 
   identify(event: IdentifyEventType) {
+    if (this.brazeProfileId === undefined) {
+      return event;
+    }
+
     //check to see if anything has changed.
     //if it hasn't changed don't send event
     const identical =
@@ -99,9 +122,7 @@ export class BrazePlugin extends DestinationPlugin {
     ) {
       return;
     } else {
-      if (event.userId !== undefined && event.userId !== null) {
-        Braze.changeUser(event.userId);
-      }
+      Braze.changeUser(this.brazeProfileId);
 
       if (event.traits?.birthday !== undefined) {
         const birthday = new Date(event.traits.birthday);
@@ -167,6 +188,7 @@ export class BrazePlugin extends DestinationPlugin {
         'gender',
         'phone',
         'address',
+        'brazeProfileId',
       ];
 
       Object.entries(event.traits ?? {}).forEach(([key, value]) => {
@@ -186,6 +208,10 @@ export class BrazePlugin extends DestinationPlugin {
   }
 
   track(event: TrackEventType) {
+    if (this.brazeProfileId === undefined) {
+      return event;
+    }
+
     const eventName = event.event;
     const revenue = this.extractRevenue(event.properties, 'revenue');
 
@@ -250,6 +276,9 @@ export class BrazePlugin extends DestinationPlugin {
   }
 
   flush() {
+    if (this.brazeProfileId === undefined) {
+      return;
+    }
     flush();
   }
 
