diff --git a/dist/perpsErrorCodes.cjs b/dist/perpsErrorCodes.cjs
index b103ef7b4d8a4b50ea217fa44620ee4c54b4f345..e6ed9ab17ddf2b1368966d8bbae196955d9f52a3 100644
--- a/dist/perpsErrorCodes.cjs
+++ b/dist/perpsErrorCodes.cjs
@@ -68,7 +68,7 @@ exports.PERPS_ERROR_CODES = {
     ORDER_TWAP_DURATION_REQUIRED: 'ORDER_TWAP_DURATION_REQUIRED', // TWAP placed without twapDuration
     ORDER_TWAP_DURATION_INVALID: 'ORDER_TWAP_DURATION_INVALID', // twapDuration not a whole number of minutes within the venue's bounds
     ORDER_SCALE_RANGE_REQUIRED: 'ORDER_SCALE_RANGE_REQUIRED', // Scale placed without both ladder bounds
-    ORDER_SCALE_RANGE_INVALID: 'ORDER_SCALE_RANGE_INVALID', // Scale ladder bounds non-positive or inverted
+    ORDER_SCALE_RANGE_INVALID: 'ORDER_SCALE_RANGE_INVALID', // Scale ladder bounds or skew are invalid
     ORDER_SCALE_COUNT_INVALID: 'ORDER_SCALE_COUNT_INVALID', // scaleNumOrders missing, non-integer, or outside the supported ladder size
     ORDER_SCALE_SIZE_TOO_SMALL: 'ORDER_SCALE_SIZE_TOO_SMALL', // Total size cannot give every ladder rung a non-zero slice
     ORDER_SCALE_NOTIONAL_TOO_SMALL: 'ORDER_SCALE_NOTIONAL_TOO_SMALL', // Ladder notional split across the rungs leaves each below the venue's per-order minimum
diff --git a/dist/perpsErrorCodes.mjs b/dist/perpsErrorCodes.mjs
index fbc0533ea403495e1ffa2dee9bc531c85f06ed92..d8d514c2c408bae53198a3f174e761e9c45b1701 100644
--- a/dist/perpsErrorCodes.mjs
+++ b/dist/perpsErrorCodes.mjs
@@ -65,7 +65,7 @@ export const PERPS_ERROR_CODES = {
     ORDER_TWAP_DURATION_REQUIRED: 'ORDER_TWAP_DURATION_REQUIRED', // TWAP placed without twapDuration
     ORDER_TWAP_DURATION_INVALID: 'ORDER_TWAP_DURATION_INVALID', // twapDuration not a whole number of minutes within the venue's bounds
     ORDER_SCALE_RANGE_REQUIRED: 'ORDER_SCALE_RANGE_REQUIRED', // Scale placed without both ladder bounds
-    ORDER_SCALE_RANGE_INVALID: 'ORDER_SCALE_RANGE_INVALID', // Scale ladder bounds non-positive or inverted
+    ORDER_SCALE_RANGE_INVALID: 'ORDER_SCALE_RANGE_INVALID', // Scale ladder bounds or skew are invalid
     ORDER_SCALE_COUNT_INVALID: 'ORDER_SCALE_COUNT_INVALID', // scaleNumOrders missing, non-integer, or outside the supported ladder size
     ORDER_SCALE_SIZE_TOO_SMALL: 'ORDER_SCALE_SIZE_TOO_SMALL', // Total size cannot give every ladder rung a non-zero slice
     ORDER_SCALE_NOTIONAL_TOO_SMALL: 'ORDER_SCALE_NOTIONAL_TOO_SMALL', // Ladder notional split across the rungs leaves each below the venue's per-order minimum
diff --git a/dist/providers/HyperLiquidProvider.cjs b/dist/providers/HyperLiquidProvider.cjs
index 4786919ca708885700c216f5fe81e0d82684a463..801264767dbdfaa370f9cbcf289842d47ef09671 100644
--- a/dist/providers/HyperLiquidProvider.cjs
+++ b/dist/providers/HyperLiquidProvider.cjs
@@ -133,6 +133,7 @@ const pickStrategyParams = (params) => ({
     scaleMinPrice: params.scaleMinPrice,
     scaleMaxPrice: params.scaleMaxPrice,
     scaleNumOrders: params.scaleNumOrders,
+    scaleSkew: params.scaleSkew,
     chaseIntervalMs: params.chaseIntervalMs,
     chaseMaxDurationMs: params.chaseMaxDurationMs,
     chaseMaxRepricings: params.chaseMaxRepricings,
@@ -6967,11 +6968,13 @@ async function _HyperLiquidProvider_prepareStrategyPlacement(params) {
     if (new Set(prices).size !== prices.length) {
         throw new Error(perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_RANGE_INVALID);
     }
-    // Throws ORDER_SCALE_SIZE_TOO_SMALL when a rung would round to nothing.
+    // Throws ORDER_SCALE_SIZE_TOO_SMALL when a rung would round to nothing,
+    // which a skew weighted far enough from even can do on its own.
     const sizes = (0, orderCalculations_js_1.splitScaleSizes)({
         totalSize: finalPositionSize,
         count,
         szDecimals,
+        skew: params.scaleSkew,
     });
     const minimumOrderSize = __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_getMinimumOrderSize).call(this);
     const cheapestRungNotional = Math.min(...sizes.map((size, index) => parseFloat(size) * parseFloat(prices[index])));
@@ -7080,7 +7083,8 @@ async function _HyperLiquidProvider_placeScaleOrder(params, context) {
     const restedRungs = statuses
         .map((status, index) => ({
         orderId: __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_readOrderIdFromStatus).call(this, status),
         size: sizes[index],
+        price: prices[index],
     }))
         .filter((rung) => rung.orderId !== undefined);
     // A ladder that came back with no IDs at all rested nothing; reporting it
@@ -7103,14 +7107,17 @@ async function _HyperLiquidProvider_placeScaleOrder(params, context) {
             statuses,
         });
     }
+    const submittedSize = isPartial
+        ? (0, hyperLiquidAdapter_js_1.formatHyperLiquidSize)({
+            size: restedRungs.reduce((total, rung) => total + parseFloat(rung.size), 0),
+            szDecimals,
+        })
+        : formattedSize;
+    const submittedValue = restedRungs.reduce((total, rung) => total + parseFloat(rung.size) * parseFloat(rung.price), 0);
     return {
         success: true,
         orderId: groupId,
         childOrderIds,
-        submittedSize: isPartial
-            ? (0, hyperLiquidAdapter_js_1.formatHyperLiquidSize)({
-                size: restedRungs.reduce((total, rung) => total + parseFloat(rung.size), 0),
-                szDecimals,
-            })
-            : formattedSize,
+        submittedSize,
+        averagePrice: String(submittedValue / parseFloat(submittedSize)),
     };
diff --git a/dist/providers/HyperLiquidProvider.mjs b/dist/providers/HyperLiquidProvider.mjs
index 9ca5f5e0626ba6d3da8e10ce0ae15a9a83cf1b52..529c3957ea15bbc75aadf34c5a07989769f25cb2 100644
--- a/dist/providers/HyperLiquidProvider.mjs
+++ b/dist/providers/HyperLiquidProvider.mjs
@@ -130,6 +130,7 @@ const pickStrategyParams = (params) => ({
     scaleMinPrice: params.scaleMinPrice,
     scaleMaxPrice: params.scaleMaxPrice,
     scaleNumOrders: params.scaleNumOrders,
+    scaleSkew: params.scaleSkew,
     chaseIntervalMs: params.chaseIntervalMs,
     chaseMaxDurationMs: params.chaseMaxDurationMs,
     chaseMaxRepricings: params.chaseMaxRepricings,
@@ -6963,11 +6964,13 @@ async function _HyperLiquidProvider_prepareStrategyPlacement(params) {
     if (new Set(prices).size !== prices.length) {
         throw new Error(PERPS_ERROR_CODES.ORDER_SCALE_RANGE_INVALID);
     }
-    // Throws ORDER_SCALE_SIZE_TOO_SMALL when a rung would round to nothing.
+    // Throws ORDER_SCALE_SIZE_TOO_SMALL when a rung would round to nothing,
+    // which a skew weighted far enough from even can do on its own.
     const sizes = splitScaleSizes({
         totalSize: finalPositionSize,
         count,
         szDecimals,
+        skew: params.scaleSkew,
     });
     const minimumOrderSize = __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_getMinimumOrderSize).call(this);
     const cheapestRungNotional = Math.min(...sizes.map((size, index) => parseFloat(size) * parseFloat(prices[index])));
@@ -7076,7 +7079,8 @@ async function _HyperLiquidProvider_placeScaleOrder(params, context) {
     const restedRungs = statuses
         .map((status, index) => ({
         orderId: __classPrivateFieldGet(this, _HyperLiquidProvider_instances, "m", _HyperLiquidProvider_readOrderIdFromStatus).call(this, status),
         size: sizes[index],
+        price: prices[index],
     }))
         .filter((rung) => rung.orderId !== undefined);
     // A ladder that came back with no IDs at all rested nothing; reporting it
@@ -7099,14 +7103,17 @@ async function _HyperLiquidProvider_placeScaleOrder(params, context) {
             statuses,
         });
     }
+    const submittedSize = isPartial
+        ? formatHyperLiquidSize({
+            size: restedRungs.reduce((total, rung) => total + parseFloat(rung.size), 0),
+            szDecimals,
+        })
+        : formattedSize;
+    const submittedValue = restedRungs.reduce((total, rung) => total + parseFloat(rung.size) * parseFloat(rung.price), 0);
     return {
         success: true,
         orderId: groupId,
         childOrderIds,
-        submittedSize: isPartial
-            ? formatHyperLiquidSize({
-                size: restedRungs.reduce((total, rung) => total + parseFloat(rung.size), 0),
-                szDecimals,
-            })
-            : formattedSize,
+        submittedSize,
+        averagePrice: String(submittedValue / parseFloat(submittedSize)),
     };
diff --git a/dist/types/index.d.cts b/dist/types/index.d.cts
index f67c373a7b53392d6a92452646aa1981671002c2..b8ee4dba5976d87606f2a33624b012288e30b64a 100644
--- a/dist/types/index.d.cts
+++ b/dist/types/index.d.cts
@@ -160,6 +160,15 @@ export type OrderParams = {
     scaleMinPrice?: string;
     scaleMaxPrice?: string;
     scaleNumOrders?: number;
+    /**
+     * How the ladder's size is weighted across its rungs. Rung weights ramp
+     * linearly from 1 at `scaleMinPrice` to this value at `scaleMaxPrice`, in that
+     * direction for both sides — a short does not flip it. Above 1 puts more size
+     * at `scaleMaxPrice`, below 1 at `scaleMinPrice`. Omitted or exactly 1 spreads
+     * the size evenly. Any finite value above 0 is accepted as given; see
+     * `splitScaleSizes` for how the sizes are allocated.
+     */
+    scaleSkew?: number;
     chaseIntervalMs?: number;
     chaseMaxDurationMs?: number;
     chaseMaxRepricings?: number;
diff --git a/dist/types/index.d.mts b/dist/types/index.d.mts
index d684f46e5ab4fff54843ea0b04ce4c3b2f0d4153..695815ec3ab9f03dc5912b7c635667dba9ae046a 100644
--- a/dist/types/index.d.mts
+++ b/dist/types/index.d.mts
@@ -160,6 +160,15 @@ export type OrderParams = {
     scaleMinPrice?: string;
     scaleMaxPrice?: string;
     scaleNumOrders?: number;
+    /**
+     * How the ladder's size is weighted across its rungs. Rung weights ramp
+     * linearly from 1 at `scaleMinPrice` to this value at `scaleMaxPrice`, in that
+     * direction for both sides — a short does not flip it. Above 1 puts more size
+     * at `scaleMaxPrice`, below 1 at `scaleMinPrice`. Omitted or exactly 1 spreads
+     * the size evenly. Any finite value above 0 is accepted as given; see
+     * `splitScaleSizes` for how the sizes are allocated.
+     */
+    scaleSkew?: number;
     chaseIntervalMs?: number;
     chaseMaxDurationMs?: number;
     chaseMaxRepricings?: number;
diff --git a/dist/types/perps-types.d.cts b/dist/types/perps-types.d.cts
index fc622addc936b54b5ef8332f06bbea1088e64bb4..b0919149f85001315dfead6203725340ffa47945 100644
--- a/dist/types/perps-types.d.cts
+++ b/dist/types/perps-types.d.cts
@@ -30,8 +30,9 @@ export type TriggerOrderType = 'stop_market' | 'stop_limit' | 'take_profit_marke
  *
  * - `twap`: slice the size over `OrderParams.twapDuration` minutes. Placed and
  *   cancelled through the venue's own TWAP endpoints, not the order book.
- * - `scale`: fan out `OrderParams.scaleNumOrders` limit orders evenly between
- *   `OrderParams.scaleMinPrice` and `OrderParams.scaleMaxPrice`.
+ * - `scale`: fan out `OrderParams.scaleNumOrders` limit orders between
+ *   `OrderParams.scaleMinPrice` and `OrderParams.scaleMaxPrice`, evenly sized
+ *   unless `OrderParams.scaleSkew` weights them along the ladder.
  * - `chase`: rest a post-only order at the near touch and re-price it as the
  *   touch moves, until it fills or the chase window closes.
  *
diff --git a/dist/types/perps-types.d.mts b/dist/types/perps-types.d.mts
index 4aa54ec98036041aa32ac6b9b1d37d22498bb1ff..ebde42d1d944e1a640327f20a7c18bd4d8a7cdad 100644
--- a/dist/types/perps-types.d.mts
+++ b/dist/types/perps-types.d.mts
@@ -30,8 +30,9 @@ export type TriggerOrderType = 'stop_market' | 'stop_limit' | 'take_profit_marke
  *
  * - `twap`: slice the size over `OrderParams.twapDuration` minutes. Placed and
  *   cancelled through the venue's own TWAP endpoints, not the order book.
- * - `scale`: fan out `OrderParams.scaleNumOrders` limit orders evenly between
- *   `OrderParams.scaleMinPrice` and `OrderParams.scaleMaxPrice`.
+ * - `scale`: fan out `OrderParams.scaleNumOrders` limit orders between
+ *   `OrderParams.scaleMinPrice` and `OrderParams.scaleMaxPrice`, evenly sized
+ *   unless `OrderParams.scaleSkew` weights them along the ladder.
  * - `chase`: rest a post-only order at the near touch and re-price it as the
  *   touch moves, until it fills or the chase window closes.
  *
diff --git a/dist/utils/hyperLiquidValidation.cjs b/dist/utils/hyperLiquidValidation.cjs
index a2687807356cb4b94484ab1892d2d326c6e21be2..2642b3ddea8881e7702dd98602c07e5d59e184cf 100644
--- a/dist/utils/hyperLiquidValidation.cjs
+++ b/dist/utils/hyperLiquidValidation.cjs
@@ -403,6 +403,7 @@ const STRATEGY_FIELD_OWNER = {
     scaleMinPrice: 'scale',
     scaleMaxPrice: 'scale',
     scaleNumOrders: 'scale',
+    scaleSkew: 'scale',
     chaseIntervalMs: 'chase',
     chaseMaxDurationMs: 'chase',
     chaseMaxRepricings: 'chase',
@@ -467,6 +468,17 @@ function validateScaleParams(params) {
             error: perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_COUNT_INVALID,
         };
     }
+    // Omitted is an even ladder, so only a supplied skew is checked. The value is
+    // taken exactly as the caller wrote it: clients coerce their input to two
+    // decimals, and rounding it again here would place a ladder weighted
+    // differently from the one the form previewed.
+    if (params.scaleSkew !== undefined &&
+        (!Number.isFinite(params.scaleSkew) || params.scaleSkew <= 0)) {
+        return {
+            isValid: false,
+            error: perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_RANGE_INVALID,
+        };
+    }
     return { isValid: true };
 }
 /**
@@ -584,6 +596,7 @@ function validateStrategyOrderParams(params) {
  * @param params.scaleMinPrice - Lowest price in a scale ladder
  * @param params.scaleMaxPrice - Highest price in a scale ladder
  * @param params.scaleNumOrders - How many orders a scale ladder fans out into
+ * @param params.scaleSkew - How a scale ladder's size is weighted across its rungs
  * @param params.chaseIntervalMs - How often a chase re-reads the touch
  * @param params.chaseMaxDurationMs - How long a chase keeps re-pricing
  * @param params.chaseMaxRepricings - Cap on a chase's cancel/replace cycles
diff --git a/dist/utils/hyperLiquidValidation.d.cts b/dist/utils/hyperLiquidValidation.d.cts
index c4b9565b86b64f051cddb62141c1adfe8e62323f..c458d94e9d1f270ce8626409172e879f1c37d77a 100644
--- a/dist/utils/hyperLiquidValidation.d.cts
+++ b/dist/utils/hyperLiquidValidation.d.cts
@@ -123,6 +123,7 @@ export type StrategyOrderValidationParams = {
     scaleMinPrice?: string;
     scaleMaxPrice?: string;
     scaleNumOrders?: number;
+    scaleSkew?: number;
     chaseIntervalMs?: number;
     chaseMaxDurationMs?: number;
     chaseMaxRepricings?: number;
@@ -152,6 +153,7 @@ export type StrategyOrderValidationParams = {
  * @param params.scaleMinPrice - Lowest price in a scale ladder
  * @param params.scaleMaxPrice - Highest price in a scale ladder
  * @param params.scaleNumOrders - How many orders a scale ladder fans out into
+ * @param params.scaleSkew - How a scale ladder's size is weighted across its rungs
  * @param params.chaseIntervalMs - How often a chase re-reads the touch
  * @param params.chaseMaxDurationMs - How long a chase keeps re-pricing
  * @param params.chaseMaxRepricings - Cap on a chase's cancel/replace cycles
diff --git a/dist/utils/hyperLiquidValidation.d.mts b/dist/utils/hyperLiquidValidation.d.mts
index 95fa47287409382c889f43aa6fc0500a2d11f1b6..760ae4e663c497c356131b11403d5ac9b44eaf36 100644
--- a/dist/utils/hyperLiquidValidation.d.mts
+++ b/dist/utils/hyperLiquidValidation.d.mts
@@ -123,6 +123,7 @@ export type StrategyOrderValidationParams = {
     scaleMinPrice?: string;
     scaleMaxPrice?: string;
     scaleNumOrders?: number;
+    scaleSkew?: number;
     chaseIntervalMs?: number;
     chaseMaxDurationMs?: number;
     chaseMaxRepricings?: number;
@@ -152,6 +153,7 @@ export type StrategyOrderValidationParams = {
  * @param params.scaleMinPrice - Lowest price in a scale ladder
  * @param params.scaleMaxPrice - Highest price in a scale ladder
  * @param params.scaleNumOrders - How many orders a scale ladder fans out into
+ * @param params.scaleSkew - How a scale ladder's size is weighted across its rungs
  * @param params.chaseIntervalMs - How often a chase re-reads the touch
  * @param params.chaseMaxDurationMs - How long a chase keeps re-pricing
  * @param params.chaseMaxRepricings - Cap on a chase's cancel/replace cycles
diff --git a/dist/utils/hyperLiquidValidation.mjs b/dist/utils/hyperLiquidValidation.mjs
index 9888e2c190aa20a58f8d859b4b61f90106a48077..556ccfbbc2ee6092f2772d3766df7b06d483dd43 100644
--- a/dist/utils/hyperLiquidValidation.mjs
+++ b/dist/utils/hyperLiquidValidation.mjs
@@ -392,6 +392,7 @@ const STRATEGY_FIELD_OWNER = {
     scaleMinPrice: 'scale',
     scaleMaxPrice: 'scale',
     scaleNumOrders: 'scale',
+    scaleSkew: 'scale',
     chaseIntervalMs: 'chase',
     chaseMaxDurationMs: 'chase',
     chaseMaxRepricings: 'chase',
@@ -456,6 +457,17 @@ function validateScaleParams(params) {
             error: PERPS_ERROR_CODES.ORDER_SCALE_COUNT_INVALID,
         };
     }
+    // Omitted is an even ladder, so only a supplied skew is checked. The value is
+    // taken exactly as the caller wrote it: clients coerce their input to two
+    // decimals, and rounding it again here would place a ladder weighted
+    // differently from the one the form previewed.
+    if (params.scaleSkew !== undefined &&
+        (!Number.isFinite(params.scaleSkew) || params.scaleSkew <= 0)) {
+        return {
+            isValid: false,
+            error: PERPS_ERROR_CODES.ORDER_SCALE_RANGE_INVALID,
+        };
+    }
     return { isValid: true };
 }
 /**
@@ -573,6 +585,7 @@ function validateStrategyOrderParams(params) {
  * @param params.scaleMinPrice - Lowest price in a scale ladder
  * @param params.scaleMaxPrice - Highest price in a scale ladder
  * @param params.scaleNumOrders - How many orders a scale ladder fans out into
+ * @param params.scaleSkew - How a scale ladder's size is weighted across its rungs
  * @param params.chaseIntervalMs - How often a chase re-reads the touch
  * @param params.chaseMaxDurationMs - How long a chase keeps re-pricing
  * @param params.chaseMaxRepricings - Cap on a chase's cancel/replace cycles
diff --git a/dist/utils/orderCalculations.cjs b/dist/utils/orderCalculations.cjs
index 4ac497ce813d5bb1e022c10a101e7b4766f0cb47..2a598bd3207b56678c06a3194dc5333e0ed8ef47 100644
--- a/dist/utils/orderCalculations.cjs
+++ b/dist/utils/orderCalculations.cjs
@@ -247,22 +247,42 @@ exports.computeScalePriceLadder = computeScalePriceLadder;
  * The split is done in whole units of the asset's size grid rather than in
  * decimal sizes: dividing and re-flooring in floating point loses a sub-unit of
  * dust on every rung, and a ladder that submits less than the size that was
- * validated is not the order the caller placed. Whatever does not divide evenly
- * goes onto the first rung, so the slices sum to exactly `totalSize`.
+ * validated is not the order the caller placed. Either allocation below sums to
+ * exactly `totalSize` in grid units.
+ *
+ * This is the one place the ladder's sizes are decided. Clients previewing a
+ * scale placement must call it rather than reproduce the ramp, or the preview
+ * and the placement will disagree at the rounding.
+ *
+ * **Even (no `skew`, or `skew` exactly 1).** Every rung gets `floor(total /
+ * count)` units and whatever does not divide evenly goes onto the first rung —
+ * 11 units across 3 rungs is `5, 3, 3`, not three equal slices.
+ *
+ * **Skewed.** Rung weights ramp linearly from 1 at index 0 to `skew` at the last
+ * index, in ladder order — which is ascending price, `scaleMinPrice` to
+ * `scaleMaxPrice`, for a buy and a sell alike. Each rung takes
+ * `floor(weight / sumOfWeights * totalUnits)` units, and the units left over go
+ * to the rungs with the largest discarded fraction, ties broken by ascending
+ * index. The leftover is deliberately *not* dumped on the first rung the way the
+ * even split does it: on a `skew` above 1 that would push size back to the end
+ * of the ladder the caller weighted away from.
  *
  * The total is expected to sit on the grid already — `calculateFinalPositionSize`
  * floors it there — so rounding onto the grid here only absorbs representation
  * error. A total too small to give every rung a whole unit is rejected: placing
- * fewer orders than asked for would silently change the strategy.
+ * fewer orders than asked for would silently change the strategy. A `skew` far
+ * enough from 1 can starve a rung the same way, and is rejected the same way.
  *
  * @param params - Split parameters.
  * @param params.totalSize - Total size to distribute.
  * @param params.count - Number of rungs.
  * @param params.szDecimals - The asset's size decimal precision.
+ * @param params.skew - Optional size weighting across the ladder; any finite
+ * value above 0, used exactly as given.
  * @returns One size string per rung, in ladder order.
  */
 function splitScaleSizes(params) {
-    const { totalSize, count, szDecimals } = params;
+    const { totalSize, count, szDecimals, skew } = params;
     // Checked here as well as in `computeScalePriceLadder`: this is exported on
     // its own, and a count of zero would otherwise return an empty split while a
     // fractional one would return slices that do not sum to the total.
@@ -271,19 +291,76 @@ function splitScaleSizes(params) {
         count > orderTypes_js_1.SCALE_ORDER_COUNT.max) {
         throw new Error(perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_COUNT_INVALID);
     }
+    // Checked here rather than left to the arithmetic: a non-finite or
+    // non-positive skew produces weights that are NaN or run negative, and either
+    // one would come back as a ladder of zero-size rungs instead of a rejection.
+    if (skew !== undefined && (!Number.isFinite(skew) || skew <= 0)) {
+        throw new Error(perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_RANGE_INVALID);
+    }
     const multiplier = Math.pow(10, szDecimals);
     const totalUnits = Math.round(totalSize * multiplier);
     if (!Number.isSafeInteger(totalUnits) || totalUnits < count) {
         throw new Error(perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_SIZE_TOO_SMALL);
     }
+    const unitsPerRung = skew === undefined || skew === 1
+        ? splitUnitsEvenly({ totalUnits, count })
+        : splitUnitsBySkew({ totalUnits, count, skew });
+    // A rung the ramp starved of every unit would be submitted as a zero-size
+    // order. That is the same failure the total-size check above rejects, and it
+    // is reported the same way, so a caller reads one reason for "this ladder
+    // cannot be cut this finely" rather than two.
+    if (unitsPerRung.some((units) => units === 0)) {
+        throw new Error(perpsErrorCodes_js_1.PERPS_ERROR_CODES.ORDER_SCALE_SIZE_TOO_SMALL);
+    }
+    return unitsPerRung.map((units) => (0, hyperLiquidAdapter_js_1.formatHyperLiquidSize)({ size: units / multiplier, szDecimals }));
+}
+exports.splitScaleSizes = splitScaleSizes;
+/**
+ * Spread the ladder's units evenly, leftover on the first rung.
+ *
+ * @param params - Split parameters.
+ * @param params.totalUnits - Total size, in whole size-grid units.
+ * @param params.count - Number of rungs.
+ * @returns Units per rung, in ladder order.
+ */
+function splitUnitsEvenly(params) {
+    const { totalUnits, count } = params;
     const sliceUnits = Math.floor(totalUnits / count);
     const remainderUnits = totalUnits - sliceUnits * count;
-    return Array.from({ length: count }, (_unused, index) => (0, hyperLiquidAdapter_js_1.formatHyperLiquidSize)({
-        size: (index === 0 ? sliceUnits + remainderUnits : sliceUnits) / multiplier,
-        szDecimals,
-    }));
+    return Array.from({ length: count }, (_unused, index) => index === 0 ? sliceUnits + remainderUnits : sliceUnits);
+}
+/**
+ * Spread the ladder's units along a linear weight ramp.
+ *
+ * Flooring every rung leaves up to `count - 1` units unallocated, and they go to
+ * the rungs that lost the most to the floor — the standard largest-remainder
+ * allocation. Ties go to the lower index, which keeps the result a function of
+ * the inputs alone rather than of sort stability.
+ *
+ * @param params - Split parameters.
+ * @param params.totalUnits - Total size, in whole size-grid units.
+ * @param params.count - Number of rungs.
+ * @param params.skew - Weight of the last rung relative to the first.
+ * @returns Units per rung, in ladder order.
+ */
+function splitUnitsBySkew(params) {
+    const { totalUnits, count, skew } = params;
+    const weights = Array.from({ length: count }, (_unused, index) => 1 + ((skew - 1) * index) / (count - 1));
+    const weightSum = weights.reduce((sum, weight) => sum + weight, 0);
+    const ideal = weights.map((weight) => (weight / weightSum) * totalUnits);
+    const unitsPerRung = ideal.map((units) => Math.floor(units));
+    const leftoverUnits = totalUnits - unitsPerRung.reduce((sum, units) => sum + units, 0);
+    Array.from({ length: count }, (_unused, index) => index)
+        .sort((left, right) => {
+        const fractionDelta = ideal[right] - unitsPerRung[right] - (ideal[left] - unitsPerRung[left]);
+        return fractionDelta === 0 ? left - right : fractionDelta;
+    })
+        .slice(0, leftoverUnits)
+        .forEach((index) => {
+        unitsPerRung[index] += 1;
+    });
+    return unitsPerRung;
 }
-exports.splitScaleSizes = splitScaleSizes;
 /**
  * Calculates final position size using USD as source of truth with price validation
  *
diff --git a/dist/utils/orderCalculations.d.cts b/dist/utils/orderCalculations.d.cts
index 315e78f2e81e68e6d25d02a998226d9aec8d9c9f..1690154642dd1bcd0593d5d722c4733f11d60306 100644
--- a/dist/utils/orderCalculations.d.cts
+++ b/dist/utils/orderCalculations.d.cts
@@ -177,24 +177,45 @@ export declare function computeScalePriceLadder(params: {
  * The split is done in whole units of the asset's size grid rather than in
  * decimal sizes: dividing and re-flooring in floating point loses a sub-unit of
  * dust on every rung, and a ladder that submits less than the size that was
- * validated is not the order the caller placed. Whatever does not divide evenly
- * goes onto the first rung, so the slices sum to exactly `totalSize`.
+ * validated is not the order the caller placed. Either allocation below sums to
+ * exactly `totalSize` in grid units.
+ *
+ * This is the one place the ladder's sizes are decided. Clients previewing a
+ * scale placement must call it rather than reproduce the ramp, or the preview
+ * and the placement will disagree at the rounding.
+ *
+ * **Even (no `skew`, or `skew` exactly 1).** Every rung gets `floor(total /
+ * count)` units and whatever does not divide evenly goes onto the first rung —
+ * 11 units across 3 rungs is `5, 3, 3`, not three equal slices.
+ *
+ * **Skewed.** Rung weights ramp linearly from 1 at index 0 to `skew` at the last
+ * index, in ladder order — which is ascending price, `scaleMinPrice` to
+ * `scaleMaxPrice`, for a buy and a sell alike. Each rung takes
+ * `floor(weight / sumOfWeights * totalUnits)` units, and the units left over go
+ * to the rungs with the largest discarded fraction, ties broken by ascending
+ * index. The leftover is deliberately *not* dumped on the first rung the way the
+ * even split does it: on a `skew` above 1 that would push size back to the end
+ * of the ladder the caller weighted away from.
  *
  * The total is expected to sit on the grid already — `calculateFinalPositionSize`
  * floors it there — so rounding onto the grid here only absorbs representation
  * error. A total too small to give every rung a whole unit is rejected: placing
- * fewer orders than asked for would silently change the strategy.
+ * fewer orders than asked for would silently change the strategy. A `skew` far
+ * enough from 1 can starve a rung the same way, and is rejected the same way.
  *
  * @param params - Split parameters.
  * @param params.totalSize - Total size to distribute.
  * @param params.count - Number of rungs.
  * @param params.szDecimals - The asset's size decimal precision.
+ * @param params.skew - Optional size weighting across the ladder; any finite
+ * value above 0, used exactly as given.
  * @returns One size string per rung, in ladder order.
  */
 export declare function splitScaleSizes(params: {
     totalSize: number;
     count: number;
     szDecimals: number;
+    skew?: number;
 }): string[];
 /**
  * Calculates final position size using USD as source of truth with price validation
diff --git a/dist/utils/orderCalculations.d.mts b/dist/utils/orderCalculations.d.mts
index 600663ea9f94eb29e5ba9594b872be57add3042a..7bb70f793228cb0e23643552e855f280207316ff 100644
--- a/dist/utils/orderCalculations.d.mts
+++ b/dist/utils/orderCalculations.d.mts
@@ -177,24 +177,45 @@ export declare function computeScalePriceLadder(params: {
  * The split is done in whole units of the asset's size grid rather than in
  * decimal sizes: dividing and re-flooring in floating point loses a sub-unit of
  * dust on every rung, and a ladder that submits less than the size that was
- * validated is not the order the caller placed. Whatever does not divide evenly
- * goes onto the first rung, so the slices sum to exactly `totalSize`.
+ * validated is not the order the caller placed. Either allocation below sums to
+ * exactly `totalSize` in grid units.
+ *
+ * This is the one place the ladder's sizes are decided. Clients previewing a
+ * scale placement must call it rather than reproduce the ramp, or the preview
+ * and the placement will disagree at the rounding.
+ *
+ * **Even (no `skew`, or `skew` exactly 1).** Every rung gets `floor(total /
+ * count)` units and whatever does not divide evenly goes onto the first rung —
+ * 11 units across 3 rungs is `5, 3, 3`, not three equal slices.
+ *
+ * **Skewed.** Rung weights ramp linearly from 1 at index 0 to `skew` at the last
+ * index, in ladder order — which is ascending price, `scaleMinPrice` to
+ * `scaleMaxPrice`, for a buy and a sell alike. Each rung takes
+ * `floor(weight / sumOfWeights * totalUnits)` units, and the units left over go
+ * to the rungs with the largest discarded fraction, ties broken by ascending
+ * index. The leftover is deliberately *not* dumped on the first rung the way the
+ * even split does it: on a `skew` above 1 that would push size back to the end
+ * of the ladder the caller weighted away from.
  *
  * The total is expected to sit on the grid already — `calculateFinalPositionSize`
  * floors it there — so rounding onto the grid here only absorbs representation
  * error. A total too small to give every rung a whole unit is rejected: placing
- * fewer orders than asked for would silently change the strategy.
+ * fewer orders than asked for would silently change the strategy. A `skew` far
+ * enough from 1 can starve a rung the same way, and is rejected the same way.
  *
  * @param params - Split parameters.
  * @param params.totalSize - Total size to distribute.
  * @param params.count - Number of rungs.
  * @param params.szDecimals - The asset's size decimal precision.
+ * @param params.skew - Optional size weighting across the ladder; any finite
+ * value above 0, used exactly as given.
  * @returns One size string per rung, in ladder order.
  */
 export declare function splitScaleSizes(params: {
     totalSize: number;
     count: number;
     szDecimals: number;
+    skew?: number;
 }): string[];
 /**
  * Calculates final position size using USD as source of truth with price validation
diff --git a/dist/utils/orderCalculations.mjs b/dist/utils/orderCalculations.mjs
index 0a5a4c0e00f8cb091d498e4d3a235f216efe5b81..f12bae309cccd7e90d40efc4f1f7d6df9fd25ad5 100644
--- a/dist/utils/orderCalculations.mjs
+++ b/dist/utils/orderCalculations.mjs
@@ -237,22 +237,42 @@ export function computeScalePriceLadder(params) {
  * The split is done in whole units of the asset's size grid rather than in
  * decimal sizes: dividing and re-flooring in floating point loses a sub-unit of
  * dust on every rung, and a ladder that submits less than the size that was
- * validated is not the order the caller placed. Whatever does not divide evenly
- * goes onto the first rung, so the slices sum to exactly `totalSize`.
+ * validated is not the order the caller placed. Either allocation below sums to
+ * exactly `totalSize` in grid units.
+ *
+ * This is the one place the ladder's sizes are decided. Clients previewing a
+ * scale placement must call it rather than reproduce the ramp, or the preview
+ * and the placement will disagree at the rounding.
+ *
+ * **Even (no `skew`, or `skew` exactly 1).** Every rung gets `floor(total /
+ * count)` units and whatever does not divide evenly goes onto the first rung —
+ * 11 units across 3 rungs is `5, 3, 3`, not three equal slices.
+ *
+ * **Skewed.** Rung weights ramp linearly from 1 at index 0 to `skew` at the last
+ * index, in ladder order — which is ascending price, `scaleMinPrice` to
+ * `scaleMaxPrice`, for a buy and a sell alike. Each rung takes
+ * `floor(weight / sumOfWeights * totalUnits)` units, and the units left over go
+ * to the rungs with the largest discarded fraction, ties broken by ascending
+ * index. The leftover is deliberately *not* dumped on the first rung the way the
+ * even split does it: on a `skew` above 1 that would push size back to the end
+ * of the ladder the caller weighted away from.
  *
  * The total is expected to sit on the grid already — `calculateFinalPositionSize`
  * floors it there — so rounding onto the grid here only absorbs representation
  * error. A total too small to give every rung a whole unit is rejected: placing
- * fewer orders than asked for would silently change the strategy.
+ * fewer orders than asked for would silently change the strategy. A `skew` far
+ * enough from 1 can starve a rung the same way, and is rejected the same way.
  *
  * @param params - Split parameters.
  * @param params.totalSize - Total size to distribute.
  * @param params.count - Number of rungs.
  * @param params.szDecimals - The asset's size decimal precision.
+ * @param params.skew - Optional size weighting across the ladder; any finite
+ * value above 0, used exactly as given.
  * @returns One size string per rung, in ladder order.
  */
 export function splitScaleSizes(params) {
-    const { totalSize, count, szDecimals } = params;
+    const { totalSize, count, szDecimals, skew } = params;
     // Checked here as well as in `computeScalePriceLadder`: this is exported on
     // its own, and a count of zero would otherwise return an empty split while a
     // fractional one would return slices that do not sum to the total.
@@ -261,17 +281,74 @@ export function splitScaleSizes(params) {
         count > SCALE_ORDER_COUNT.max) {
         throw new Error(PERPS_ERROR_CODES.ORDER_SCALE_COUNT_INVALID);
     }
+    // Checked here rather than left to the arithmetic: a non-finite or
+    // non-positive skew produces weights that are NaN or run negative, and either
+    // one would come back as a ladder of zero-size rungs instead of a rejection.
+    if (skew !== undefined && (!Number.isFinite(skew) || skew <= 0)) {
+        throw new Error(PERPS_ERROR_CODES.ORDER_SCALE_RANGE_INVALID);
+    }
     const multiplier = Math.pow(10, szDecimals);
     const totalUnits = Math.round(totalSize * multiplier);
     if (!Number.isSafeInteger(totalUnits) || totalUnits < count) {
         throw new Error(PERPS_ERROR_CODES.ORDER_SCALE_SIZE_TOO_SMALL);
     }
+    const unitsPerRung = skew === undefined || skew === 1
+        ? splitUnitsEvenly({ totalUnits, count })
+        : splitUnitsBySkew({ totalUnits, count, skew });
+    // A rung the ramp starved of every unit would be submitted as a zero-size
+    // order. That is the same failure the total-size check above rejects, and it
+    // is reported the same way, so a caller reads one reason for "this ladder
+    // cannot be cut this finely" rather than two.
+    if (unitsPerRung.some((units) => units === 0)) {
+        throw new Error(PERPS_ERROR_CODES.ORDER_SCALE_SIZE_TOO_SMALL);
+    }
+    return unitsPerRung.map((units) => formatHyperLiquidSize({ size: units / multiplier, szDecimals }));
+}
+/**
+ * Spread the ladder's units evenly, leftover on the first rung.
+ *
+ * @param params - Split parameters.
+ * @param params.totalUnits - Total size, in whole size-grid units.
+ * @param params.count - Number of rungs.
+ * @returns Units per rung, in ladder order.
+ */
+function splitUnitsEvenly(params) {
+    const { totalUnits, count } = params;
     const sliceUnits = Math.floor(totalUnits / count);
     const remainderUnits = totalUnits - sliceUnits * count;
-    return Array.from({ length: count }, (_unused, index) => formatHyperLiquidSize({
-        size: (index === 0 ? sliceUnits + remainderUnits : sliceUnits) / multiplier,
-        szDecimals,
-    }));
+    return Array.from({ length: count }, (_unused, index) => index === 0 ? sliceUnits + remainderUnits : sliceUnits);
+}
+/**
+ * Spread the ladder's units along a linear weight ramp.
+ *
+ * Flooring every rung leaves up to `count - 1` units unallocated, and they go to
+ * the rungs that lost the most to the floor — the standard largest-remainder
+ * allocation. Ties go to the lower index, which keeps the result a function of
+ * the inputs alone rather than of sort stability.
+ *
+ * @param params - Split parameters.
+ * @param params.totalUnits - Total size, in whole size-grid units.
+ * @param params.count - Number of rungs.
+ * @param params.skew - Weight of the last rung relative to the first.
+ * @returns Units per rung, in ladder order.
+ */
+function splitUnitsBySkew(params) {
+    const { totalUnits, count, skew } = params;
+    const weights = Array.from({ length: count }, (_unused, index) => 1 + ((skew - 1) * index) / (count - 1));
+    const weightSum = weights.reduce((sum, weight) => sum + weight, 0);
+    const ideal = weights.map((weight) => (weight / weightSum) * totalUnits);
+    const unitsPerRung = ideal.map((units) => Math.floor(units));
+    const leftoverUnits = totalUnits - unitsPerRung.reduce((sum, units) => sum + units, 0);
+    Array.from({ length: count }, (_unused, index) => index)
+        .sort((left, right) => {
+        const fractionDelta = ideal[right] - unitsPerRung[right] - (ideal[left] - unitsPerRung[left]);
+        return fractionDelta === 0 ? left - right : fractionDelta;
+    })
+        .slice(0, leftoverUnits)
+        .forEach((index) => {
+        unitsPerRung[index] += 1;
+    });
+    return unitsPerRung;
 }
 /**
  * Calculates final position size using USD as source of truth with price validation
diff --git a/dist/constants/eventNames.cjs b/dist/constants/eventNames.cjs
index 127a7ef52..f5439e1a0 100644
--- a/dist/constants/eventNames.cjs
+++ b/dist/constants/eventNames.cjs
@@ -25,6 +25,10 @@ exports.PERPS_EVENT_PROPERTY = {
     MARGIN_USED: 'margin_used',
     ORDER_TYPE: 'order_type', // lowercase per dashboard
     ORDER_TIMESTAMP: 'order_timestamp',
+    SCALE_ORDER_COUNT: 'scale_order_count',
+    SCALE_RANGE_PCT: 'scale_range_pct',
+    SCALE_SKEW: 'scale_skew',
+    REDUCE_ONLY: 'reduce_only',
     LIMIT_PRICE: 'limit_price',
     FEES: 'fees',
     FEE: 'fee',
@@ -441,6 +445,9 @@ exports.PERPS_EVENT_VALUE = {
         SLIPPAGE_CONFIG_OPENED: 'slippage_config_opened',
         SLIPPAGE_CONFIG_CHANGED: 'slippage_config_changed',
         SLIPPAGE_LIMIT_BLOCKED_ORDER: 'slippage_limit_blocked_order',
+        SCALE_VALIDATION_ERROR_SHOWN: 'scale_validation_error_shown',
+        SCALE_CONFIG_CHANGED: 'scale_config_changed',
+        SCALE_PREVIEW_EXPANDED: 'scale_preview_expanded',
         // Auto Close TP/SL RoE sign toggle
         TPSL_ROE_SIGN_TOGGLED: 'tpsl_roe_sign_toggled',
         // Discovery analytics
@@ -544,6 +551,10 @@ exports.PERPS_EVENT_VALUE = {
     SETTING_TYPE: {
         LEVERAGE: 'leverage',
         SLIPPAGE: 'slippage',
+        SCALE_START_PRICE: 'start_price',
+        SCALE_END_PRICE: 'end_price',
+        SCALE_TOTAL_ORDERS: 'total_orders',
+        SCALE_SIZE_SKEW: 'size_skew',
     },
     SCREEN_NAME: {
         CONNECTION_ERROR: 'connection_error',
diff --git a/dist/constants/eventNames.mjs b/dist/constants/eventNames.mjs
index 8fa620019..a97b022a7 100644
--- a/dist/constants/eventNames.mjs
+++ b/dist/constants/eventNames.mjs
@@ -22,6 +22,10 @@ export const PERPS_EVENT_PROPERTY = {
     MARGIN_USED: 'margin_used',
     ORDER_TYPE: 'order_type', // lowercase per dashboard
     ORDER_TIMESTAMP: 'order_timestamp',
+    SCALE_ORDER_COUNT: 'scale_order_count',
+    SCALE_RANGE_PCT: 'scale_range_pct',
+    SCALE_SKEW: 'scale_skew',
+    REDUCE_ONLY: 'reduce_only',
     LIMIT_PRICE: 'limit_price',
     FEES: 'fees',
     FEE: 'fee',
@@ -438,6 +442,9 @@ export const PERPS_EVENT_VALUE = {
         SLIPPAGE_CONFIG_OPENED: 'slippage_config_opened',
         SLIPPAGE_CONFIG_CHANGED: 'slippage_config_changed',
         SLIPPAGE_LIMIT_BLOCKED_ORDER: 'slippage_limit_blocked_order',
+        SCALE_VALIDATION_ERROR_SHOWN: 'scale_validation_error_shown',
+        SCALE_CONFIG_CHANGED: 'scale_config_changed',
+        SCALE_PREVIEW_EXPANDED: 'scale_preview_expanded',
         // Auto Close TP/SL RoE sign toggle
         TPSL_ROE_SIGN_TOGGLED: 'tpsl_roe_sign_toggled',
         // Discovery analytics
@@ -541,6 +548,10 @@ export const PERPS_EVENT_VALUE = {
     SETTING_TYPE: {
         LEVERAGE: 'leverage',
         SLIPPAGE: 'slippage',
+        SCALE_START_PRICE: 'start_price',
+        SCALE_END_PRICE: 'end_price',
+        SCALE_TOTAL_ORDERS: 'total_orders',
+        SCALE_SIZE_SKEW: 'size_skew',
     },
     SCREEN_NAME: {
         CONNECTION_ERROR: 'connection_error',
diff --git a/dist/constants/eventNames.d.cts b/dist/constants/eventNames.d.cts
index 4471a1fed..5cb61623d 100644
--- a/dist/constants/eventNames.d.cts
+++ b/dist/constants/eventNames.d.cts
@@ -18,6 +18,10 @@ export declare const PERPS_EVENT_PROPERTY: {
     readonly MARGIN_USED: "margin_used";
     readonly ORDER_TYPE: "order_type";
     readonly ORDER_TIMESTAMP: "order_timestamp";
+    readonly SCALE_ORDER_COUNT: "scale_order_count";
+    readonly SCALE_RANGE_PCT: "scale_range_pct";
+    readonly SCALE_SKEW: "scale_skew";
+    readonly REDUCE_ONLY: "reduce_only";
     readonly LIMIT_PRICE: "limit_price";
     readonly FEES: "fees";
     readonly FEE: "fee";
@@ -371,6 +375,9 @@ export declare const PERPS_EVENT_VALUE: {
         readonly SLIPPAGE_CONFIG_OPENED: "slippage_config_opened";
         readonly SLIPPAGE_CONFIG_CHANGED: "slippage_config_changed";
         readonly SLIPPAGE_LIMIT_BLOCKED_ORDER: "slippage_limit_blocked_order";
+        readonly SCALE_VALIDATION_ERROR_SHOWN: "scale_validation_error_shown";
+        readonly SCALE_CONFIG_CHANGED: "scale_config_changed";
+        readonly SCALE_PREVIEW_EXPANDED: "scale_preview_expanded";
         readonly TPSL_ROE_SIGN_TOGGLED: "tpsl_roe_sign_toggled";
         readonly MARKET_LIST_FILTER: "market_list_filter";
         readonly SORT_APPLIED: "sort_applied";
@@ -461,6 +468,10 @@ export declare const PERPS_EVENT_VALUE: {
     readonly SETTING_TYPE: {
         readonly LEVERAGE: "leverage";
         readonly SLIPPAGE: "slippage";
+        readonly SCALE_START_PRICE: "start_price";
+        readonly SCALE_END_PRICE: "end_price";
+        readonly SCALE_TOTAL_ORDERS: "total_orders";
+        readonly SCALE_SIZE_SKEW: "size_skew";
     };
     readonly SCREEN_NAME: {
         readonly CONNECTION_ERROR: "connection_error";
diff --git a/dist/constants/eventNames.d.mts b/dist/constants/eventNames.d.mts
index 4471a1fed..5cb61623d 100644
--- a/dist/constants/eventNames.d.mts
+++ b/dist/constants/eventNames.d.mts
@@ -18,6 +18,10 @@ export declare const PERPS_EVENT_PROPERTY: {
     readonly MARGIN_USED: "margin_used";
     readonly ORDER_TYPE: "order_type";
     readonly ORDER_TIMESTAMP: "order_timestamp";
+    readonly SCALE_ORDER_COUNT: "scale_order_count";
+    readonly SCALE_RANGE_PCT: "scale_range_pct";
+    readonly SCALE_SKEW: "scale_skew";
+    readonly REDUCE_ONLY: "reduce_only";
     readonly LIMIT_PRICE: "limit_price";
     readonly FEES: "fees";
     readonly FEE: "fee";
@@ -371,6 +375,9 @@ export declare const PERPS_EVENT_VALUE: {
         readonly SLIPPAGE_CONFIG_OPENED: "slippage_config_opened";
         readonly SLIPPAGE_CONFIG_CHANGED: "slippage_config_changed";
         readonly SLIPPAGE_LIMIT_BLOCKED_ORDER: "slippage_limit_blocked_order";
+        readonly SCALE_VALIDATION_ERROR_SHOWN: "scale_validation_error_shown";
+        readonly SCALE_CONFIG_CHANGED: "scale_config_changed";
+        readonly SCALE_PREVIEW_EXPANDED: "scale_preview_expanded";
         readonly TPSL_ROE_SIGN_TOGGLED: "tpsl_roe_sign_toggled";
         readonly MARKET_LIST_FILTER: "market_list_filter";
         readonly SORT_APPLIED: "sort_applied";
@@ -461,6 +468,10 @@ export declare const PERPS_EVENT_VALUE: {
     readonly SETTING_TYPE: {
         readonly LEVERAGE: "leverage";
         readonly SLIPPAGE: "slippage";
+        readonly SCALE_START_PRICE: "start_price";
+        readonly SCALE_END_PRICE: "end_price";
+        readonly SCALE_TOTAL_ORDERS: "total_orders";
+        readonly SCALE_SIZE_SKEW: "size_skew";
     };
     readonly SCREEN_NAME: {
         readonly CONNECTION_ERROR: "connection_error";
diff --git a/dist/services/TradingService.cjs b/dist/services/TradingService.cjs
index c2267c09e..4e4a23d8b 100644
--- a/dist/services/TradingService.cjs
+++ b/dist/services/TradingService.cjs
@@ -1360,6 +1360,7 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
     const status = result?.success === true
         ? eventNames_js_1.PERPS_EVENT_VALUE.STATUS.EXECUTED
         : eventNames_js_1.PERPS_EVENT_VALUE.STATUS.FAILED;
+    const trackedOrderSize = parseFloat(result?.filledSize ?? result?.submittedSize ?? params.size);
     // Build base properties
     const properties = {
         [eventNames_js_1.PERPS_EVENT_PROPERTY.STATUS]: status,
@@ -1369,7 +1370,7 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
             : eventNames_js_1.PERPS_EVENT_VALUE.DIRECTION.SHORT,
         [eventNames_js_1.PERPS_EVENT_PROPERTY.ORDER_TYPE]: params.orderType,
         [eventNames_js_1.PERPS_EVENT_PROPERTY.LEVERAGE]: parseFloat(String(params.leverage ?? 1)),
-        [eventNames_js_1.PERPS_EVENT_PROPERTY.ORDER_SIZE]: parseFloat(result?.filledSize ?? params.size),
+        [eventNames_js_1.PERPS_EVENT_PROPERTY.ORDER_SIZE]: trackedOrderSize,
         [eventNames_js_1.PERPS_EVENT_PROPERTY.COMPLETION_DURATION]: duration,
     };
     // Add optional properties
@@ -1385,6 +1385,23 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
     if ((0, orderTypes_js_2.isLimitExecutionOrderType)(params.orderType) && params.price) {
         properties[eventNames_js_1.PERPS_EVENT_PROPERTY.LIMIT_PRICE] = parseFloat(params.price);
     }
+    if (params.orderType === 'scale') {
+        properties[eventNames_js_1.PERPS_EVENT_PROPERTY.SCALE_ORDER_COUNT] =
+            params.scaleNumOrders;
+        const minPrice = Number(params.scaleMinPrice);
+        const maxPrice = Number(params.scaleMaxPrice);
+        if (Number.isFinite(minPrice) &&
+            Number.isFinite(maxPrice) &&
+            minPrice > 0 &&
+            maxPrice > minPrice) {
+            properties[eventNames_js_1.PERPS_EVENT_PROPERTY.SCALE_RANGE_PCT] =
+                ((maxPrice - minPrice) / minPrice) * 100;
+        }
+        properties[eventNames_js_1.PERPS_EVENT_PROPERTY.SCALE_SKEW] =
+            params.scaleSkew ?? 1;
+        properties[eventNames_js_1.PERPS_EVENT_PROPERTY.REDUCE_ONLY] =
+            params.reduceOnly ?? false;
+    }
     if (params.trackingData?.source) {
         properties[eventNames_js_1.PERPS_EVENT_PROPERTY.SOURCE] = params.trackingData.source;
     }
@@ -1420,12 +1437,11 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
             eventNames_js_1.PERPS_EVENT_VALUE.MM_PAY_TOKEN.PERPS_BALANCE;
     }
     // Calculate order value in USD (size * price)
-    const orderSize = parseFloat(result?.filledSize ?? params.size);
     const assetPrice = result?.averagePrice
         ? parseFloat(result.averagePrice)
         : params.trackingData?.marketPrice;
-    if (assetPrice && orderSize) {
-        properties[eventNames_js_1.PERPS_EVENT_PROPERTY.ORDER_VALUE] = orderSize * assetPrice;
+    if (assetPrice && trackedOrderSize) {
+        properties[eventNames_js_1.PERPS_EVENT_PROPERTY.ORDER_VALUE] = trackedOrderSize * assetPrice;
     }
     // Add success-specific properties
     if (status === eventNames_js_1.PERPS_EVENT_VALUE.STATUS.EXECUTED) {
diff --git a/dist/services/TradingService.mjs b/dist/services/TradingService.mjs
index 1944e2815..cff4e7c4b 100644
--- a/dist/services/TradingService.mjs
+++ b/dist/services/TradingService.mjs
@@ -1356,6 +1356,7 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
     const status = result?.success === true
         ? PERPS_EVENT_VALUE.STATUS.EXECUTED
         : PERPS_EVENT_VALUE.STATUS.FAILED;
+    const trackedOrderSize = parseFloat(result?.filledSize ?? result?.submittedSize ?? params.size);
     // Build base properties
     const properties = {
         [PERPS_EVENT_PROPERTY.STATUS]: status,
@@ -1365,7 +1366,7 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
             : PERPS_EVENT_VALUE.DIRECTION.SHORT,
         [PERPS_EVENT_PROPERTY.ORDER_TYPE]: params.orderType,
         [PERPS_EVENT_PROPERTY.LEVERAGE]: parseFloat(String(params.leverage ?? 1)),
-        [PERPS_EVENT_PROPERTY.ORDER_SIZE]: parseFloat(result?.filledSize ?? params.size),
+        [PERPS_EVENT_PROPERTY.ORDER_SIZE]: trackedOrderSize,
         [PERPS_EVENT_PROPERTY.COMPLETION_DURATION]: duration,
     };
     // Add optional properties
@@ -1381,6 +1381,22 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
     if (isLimitExecutionOrderType(params.orderType) && params.price) {
         properties[PERPS_EVENT_PROPERTY.LIMIT_PRICE] = parseFloat(params.price);
     }
+    if (params.orderType === 'scale') {
+        properties[PERPS_EVENT_PROPERTY.SCALE_ORDER_COUNT] =
+            params.scaleNumOrders;
+        const minPrice = Number(params.scaleMinPrice);
+        const maxPrice = Number(params.scaleMaxPrice);
+        if (Number.isFinite(minPrice) &&
+            Number.isFinite(maxPrice) &&
+            minPrice > 0 &&
+            maxPrice > minPrice) {
+            properties[PERPS_EVENT_PROPERTY.SCALE_RANGE_PCT] =
+                ((maxPrice - minPrice) / minPrice) * 100;
+        }
+        properties[PERPS_EVENT_PROPERTY.SCALE_SKEW] = params.scaleSkew ?? 1;
+        properties[PERPS_EVENT_PROPERTY.REDUCE_ONLY] =
+            params.reduceOnly ?? false;
+    }
     if (params.trackingData?.source) {
         properties[PERPS_EVENT_PROPERTY.SOURCE] = params.trackingData.source;
     }
@@ -1416,12 +1432,11 @@ _TradingService_deps = new WeakMap(), _TradingService_controllerDeps = new WeakM
             PERPS_EVENT_VALUE.MM_PAY_TOKEN.PERPS_BALANCE;
     }
     // Calculate order value in USD (size * price)
-    const orderSize = parseFloat(result?.filledSize ?? params.size);
     const assetPrice = result?.averagePrice
         ? parseFloat(result.averagePrice)
         : params.trackingData?.marketPrice;
-    if (assetPrice && orderSize) {
-        properties[PERPS_EVENT_PROPERTY.ORDER_VALUE] = orderSize * assetPrice;
+    if (assetPrice && trackedOrderSize) {
+        properties[PERPS_EVENT_PROPERTY.ORDER_VALUE] = trackedOrderSize * assetPrice;
     }
     // Add success-specific properties
     if (status === PERPS_EVENT_VALUE.STATUS.EXECUTED) {
