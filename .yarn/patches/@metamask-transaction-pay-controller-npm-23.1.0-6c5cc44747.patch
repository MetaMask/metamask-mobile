diff --git a/dist/strategy/fiat/fiat-submit.cjs b/dist/strategy/fiat/fiat-submit.cjs
index 8f46b26983404ee0625ebce81a87d1646d0f1c93..1aa641ac84c58e11f7caace6bcf6f26be95c2645 100644
--- a/dist/strategy/fiat/fiat-submit.cjs
+++ b/dist/strategy/fiat/fiat-submit.cjs
@@ -13,7 +13,6 @@ const utils_2 = require("./utils.cjs");
 const log = (0, utils_1.createModuleLogger)(logger_1.projectLogger, 'fiat-submit');
 const ORDER_POLL_INTERVAL_MS = 1000;
 const ORDER_POLL_TIMEOUT_MS = 10 * 60 * 1000;
-const MAX_SLIPPAGE_PERCENT = 5;
 const TERMINAL_FAILURE_STATUSES = [
     ramps_controller_1.RampsOrderStatus.Cancelled,
     ramps_controller_1.RampsOrderStatus.Failed,
@@ -110,36 +109,6 @@ function validateOrderAsset({ expectedAsset, orderCrypto, transactionId, }) {
             `expected ${expectedChainId}, got ${orderChainId}`);
     }
 }
-/**
- * Validates that the re-quoted relay target output hasn't drifted beyond the
- * acceptable slippage threshold compared to the original quote shown to the user.
- *
- * @param options - The validation options.
- * @param options.originalTargetRaw - Raw target amount from the original relay quote.
- * @param options.reQuotedTargetRaw - Raw target amount from the re-quoted relay.
- * @param options.transactionId - Transaction ID for error reporting.
- */
-function validateRelaySlippage({ originalTargetRaw, reQuotedTargetRaw, transactionId, }) {
-    const original = new bignumber_js_1.BigNumber(originalTargetRaw);
-    const reQuoted = new bignumber_js_1.BigNumber(reQuotedTargetRaw);
-    if (!original.gt(0) || !reQuoted.gt(0)) {
-        return;
-    }
-    const slippagePercent = original
-        .minus(reQuoted)
-        .dividedBy(original)
-        .multipliedBy(100);
-    log('Relay slippage check', {
-        originalTargetRaw,
-        reQuotedTargetRaw,
-        slippagePercent: slippagePercent.toFixed(2),
-        transactionId,
-    });
-    if (slippagePercent.gt(MAX_SLIPPAGE_PERCENT)) {
-        throw new Error(`Relay re-quote slippage too high for transaction ` +
-            `${slippagePercent.toFixed(2)}% exceeds ${MAX_SLIPPAGE_PERCENT}% max`);
-    }
-}
 /**
  * Polls the on-ramp order until it reaches a terminal status.
  *
@@ -213,17 +182,21 @@ async function submitRelayAfterFiatCompletion({ order, request, }) {
         walletAddress,
     });
     const baseRequest = quotes[0].request;
+    const originalRelayQuote = quotes[0].original.relayQuote;
+    const targetAmountMinimum = scaleTargetAmount(originalRelayQuote, sourceAmountRaw);
     const relayRequest = {
         ...baseRequest,
-        isMaxAmount: true,
+        isMaxAmount: false,
         isPostQuote: false,
         sourceBalanceRaw: sourceAmountRaw,
         sourceTokenAmount: sourceAmountRaw,
+        targetAmountMinimum,
     };
     log('Re-quoting relay from completed fiat order', {
         completedOrderAmount: order.cryptoAmount,
         relayRequest,
         sourceAmountRaw,
+        targetAmountMinimum,
         transactionId,
     });
     const relayQuotes = await (0, relay_quotes_1.getRelayQuotes)({
@@ -235,12 +208,6 @@ async function submitRelayAfterFiatCompletion({ order, request, }) {
     if (!relayQuotes.length) {
         throw new Error('No relay quotes returned for completed fiat order');
     }
-    const originalRelayQuote = quotes[0].original.relayQuote;
-    validateRelaySlippage({
-        originalTargetRaw: originalRelayQuote.details.currencyOut.amount,
-        reQuotedTargetRaw: relayQuotes[0].original.details.currencyOut.amount,
-        transactionId,
-    });
     log('Received relay quotes for completed fiat order', {
         relayQuoteCount: relayQuotes.length,
         transactionId,
@@ -259,4 +226,16 @@ async function submitRelayAfterFiatCompletion({ order, request, }) {
     });
     return relayResult;
 }
+function scaleTargetAmount(originalRelayQuote, settledSourceAmountRaw) {
+    const originalSource = new bignumber_js_1.BigNumber(originalRelayQuote.details.currencyIn.amount);
+    const originalTarget = new bignumber_js_1.BigNumber(originalRelayQuote.details.currencyOut.amount);
+    if (!originalSource.gt(0) || !originalTarget.gt(0)) {
+        return '0';
+    }
+    return originalTarget
+        .multipliedBy(settledSourceAmountRaw)
+        .dividedBy(originalSource)
+        .integerValue(bignumber_js_1.BigNumber.ROUND_DOWN)
+        .toFixed(0);
+}
 //# sourceMappingURL=fiat-submit.cjs.map
\ No newline at end of file
diff --git a/dist/strategy/fiat/fiat-submit.mjs b/dist/strategy/fiat/fiat-submit.mjs
index fe769cb8643ee0819df37aec0f9ec64fb89d4431..3d0c9b3a00dbb13a9cc4a14fb53d093f5ccc3f3a 100644
--- a/dist/strategy/fiat/fiat-submit.mjs
+++ b/dist/strategy/fiat/fiat-submit.mjs
@@ -10,7 +10,6 @@ import { deriveFiatAssetForFiatPayment, resolveSourceAmountRaw } from "./utils.m
 const log = createModuleLogger(projectLogger, 'fiat-submit');
 const ORDER_POLL_INTERVAL_MS = 1000;
 const ORDER_POLL_TIMEOUT_MS = 10 * 60 * 1000;
-const MAX_SLIPPAGE_PERCENT = 5;
 const TERMINAL_FAILURE_STATUSES = [
     RampsOrderStatus.Cancelled,
     RampsOrderStatus.Failed,
@@ -106,36 +105,6 @@ function validateOrderAsset({ expectedAsset, orderCrypto, transactionId, }) {
             `expected ${expectedChainId}, got ${orderChainId}`);
     }
 }
-/**
- * Validates that the re-quoted relay target output hasn't drifted beyond the
- * acceptable slippage threshold compared to the original quote shown to the user.
- *
- * @param options - The validation options.
- * @param options.originalTargetRaw - Raw target amount from the original relay quote.
- * @param options.reQuotedTargetRaw - Raw target amount from the re-quoted relay.
- * @param options.transactionId - Transaction ID for error reporting.
- */
-function validateRelaySlippage({ originalTargetRaw, reQuotedTargetRaw, transactionId, }) {
-    const original = new BigNumber(originalTargetRaw);
-    const reQuoted = new BigNumber(reQuotedTargetRaw);
-    if (!original.gt(0) || !reQuoted.gt(0)) {
-        return;
-    }
-    const slippagePercent = original
-        .minus(reQuoted)
-        .dividedBy(original)
-        .multipliedBy(100);
-    log('Relay slippage check', {
-        originalTargetRaw,
-        reQuotedTargetRaw,
-        slippagePercent: slippagePercent.toFixed(2),
-        transactionId,
-    });
-    if (slippagePercent.gt(MAX_SLIPPAGE_PERCENT)) {
-        throw new Error(`Relay re-quote slippage too high for transaction ` +
-            `${slippagePercent.toFixed(2)}% exceeds ${MAX_SLIPPAGE_PERCENT}% max`);
-    }
-}
 /**
  * Polls the on-ramp order until it reaches a terminal status.
  *
@@ -209,17 +178,21 @@ async function submitRelayAfterFiatCompletion({ order, request, }) {
         walletAddress,
     });
     const baseRequest = quotes[0].request;
+    const originalRelayQuote = quotes[0].original.relayQuote;
+    const targetAmountMinimum = scaleTargetAmount(originalRelayQuote, sourceAmountRaw);
     const relayRequest = {
         ...baseRequest,
-        isMaxAmount: true,
+        isMaxAmount: false,
         isPostQuote: false,
         sourceBalanceRaw: sourceAmountRaw,
         sourceTokenAmount: sourceAmountRaw,
+        targetAmountMinimum,
     };
     log('Re-quoting relay from completed fiat order', {
         completedOrderAmount: order.cryptoAmount,
         relayRequest,
         sourceAmountRaw,
+        targetAmountMinimum,
         transactionId,
     });
     const relayQuotes = await getRelayQuotes({
@@ -231,12 +204,6 @@ async function submitRelayAfterFiatCompletion({ order, request, }) {
     if (!relayQuotes.length) {
         throw new Error('No relay quotes returned for completed fiat order');
     }
-    const originalRelayQuote = quotes[0].original.relayQuote;
-    validateRelaySlippage({
-        originalTargetRaw: originalRelayQuote.details.currencyOut.amount,
-        reQuotedTargetRaw: relayQuotes[0].original.details.currencyOut.amount,
-        transactionId,
-    });
     log('Received relay quotes for completed fiat order', {
         relayQuoteCount: relayQuotes.length,
         transactionId,
@@ -255,4 +222,16 @@ async function submitRelayAfterFiatCompletion({ order, request, }) {
     });
     return relayResult;
 }
+function scaleTargetAmount(originalRelayQuote, settledSourceAmountRaw) {
+    const originalSource = new BigNumber(originalRelayQuote.details.currencyIn.amount);
+    const originalTarget = new BigNumber(originalRelayQuote.details.currencyOut.amount);
+    if (!originalSource.gt(0) || !originalTarget.gt(0)) {
+        return '0';
+    }
+    return originalTarget
+        .multipliedBy(settledSourceAmountRaw)
+        .dividedBy(originalSource)
+        .integerValue(BigNumber.ROUND_DOWN)
+        .toFixed(0);
+}
 //# sourceMappingURL=fiat-submit.mjs.map
\ No newline at end of file
