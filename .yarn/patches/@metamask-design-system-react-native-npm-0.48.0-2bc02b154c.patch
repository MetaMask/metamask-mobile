diff --git a/dist/components/BottomSheetDialog/BottomSheetDialog.cjs b/dist/components/BottomSheetDialog/BottomSheetDialog.cjs
index 7fcd10847c9c599737d33b0961cf63c06aaf9002..7241833f6ae6c1ac7c3b0d3c42f6be171fc8bc97 100644
--- a/dist/components/BottomSheetDialog/BottomSheetDialog.cjs
+++ b/dist/components/BottomSheetDialog/BottomSheetDialog.cjs
@@ -33,6 +33,13 @@ const react_native_gesture_handler_1 = require("react-native-gesture-handler");
 const react_native_reanimated_1 = __importStar(require("react-native-reanimated"));
 const react_native_safe_area_context_1 = require("react-native-safe-area-context");
 const react_native_worklets_1 = require("react-native-worklets");
+// MM patch: capture the bare function, not the module namespace. The Worklets
+// babel plugin otherwise captures `react_native_worklets_1` (which contains the
+// WorkletsModule NativeWorklets instance) into worklet closures, and worklets
+// >=0.10 throws "Cannot copy value of type `NativeWorklets`" when serializing
+// them — fatal on Android bridgeless (kills the React host at sheet/toast open).
+const scheduleOnRN = react_native_worklets_1.scheduleOnRN;
+const withTiming = react_native_reanimated_1.withTiming;
 // Internal dependencies.
 const BottomSheetDialog_constants_1 = require("./BottomSheetDialog.constants.cjs");
 exports.BottomSheetDialog = (0, react_1.forwardRef)(({ children, isFullscreen = false, isInteractable = true, keyboardAvoidingViewEnabled = true, onClose, onOpen, style, twClassName, ...props }, ref) => {
@@ -58,10 +65,10 @@ exports.BottomSheetDialog = (0, react_1.forwardRef)(({ children, isFullscreen =
         onClose?.();
     }, [onClose]);
     const onCloseDialog = (0, react_1.useCallback)((callback) => {
-        currentYOffset.value = (0, react_native_reanimated_1.withTiming)(bottomOfDialogYValue.value, { duration: BottomSheetDialog_constants_1.DEFAULT_BOTTOMSHEETDIALOG_DISPLAY_DURATION }, () => {
-            (0, react_native_worklets_1.scheduleOnRN)(onCloseCB);
+        currentYOffset.value = withTiming(bottomOfDialogYValue.value, { duration: BottomSheetDialog_constants_1.DEFAULT_BOTTOMSHEETDIALOG_DISPLAY_DURATION }, () => {
+            scheduleOnRN(onCloseCB);
             if (callback) {
-                (0, react_native_worklets_1.scheduleOnRN)(callback);
+                scheduleOnRN(callback);
             }
         });
         // Ref values do not affect deps.
@@ -132,11 +139,11 @@ exports.BottomSheetDialog = (0, react_1.forwardRef)(({ children, isFullscreen =
             }
             const isDismissed = finalYOffset === bottomOfDialogYValue.value;
             if (isDismissed) {
-                (0, react_native_worklets_1.scheduleOnRN)(onCloseDialog);
+                scheduleOnRN(onCloseDialog);
             }
             else {
                 // Only animate dialog to a certain Y position instead
-                currentYOffset.value = (0, react_native_reanimated_1.withTiming)(finalYOffset, {
+                currentYOffset.value = withTiming(finalYOffset, {
                     duration: BottomSheetDialog_constants_1.DEFAULT_BOTTOMSHEETDIALOG_DISPLAY_DURATION,
                 });
             }
@@ -155,12 +162,12 @@ exports.BottomSheetDialog = (0, react_1.forwardRef)(({ children, isFullscreen =
         // Starts setting the Y position of the dialog to the bottom of the dialog
         currentYOffset.value = bottomOfDialogYValue.value;
         // Animate the Y position to the top of the dialog, then call onOpenCB
-        currentYOffset.value = (0, react_native_reanimated_1.withTiming)(topOfDialogYValue.value, {
+        currentYOffset.value = withTiming(topOfDialogYValue.value, {
             duration: BottomSheetDialog_constants_1.DEFAULT_BOTTOMSHEETDIALOG_DISPLAY_DURATION,
         }, () => {
-            (0, react_native_worklets_1.scheduleOnRN)(onOpenCB);
+            scheduleOnRN(onOpenCB);
             if (callback) {
-                (0, react_native_worklets_1.scheduleOnRN)(callback);
+                scheduleOnRN(callback);
             }
         });
     };
@@ -172,6 +179,19 @@ exports.BottomSheetDialog = (0, react_1.forwardRef)(({ children, isFullscreen =
     // Disable for now since network switches causes the screen to hang with this on.
     // LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);
     onDebouncedCloseDialog.cancel(), [children, onDebouncedCloseDialog]);
+    // Reanimated 4 / Fabric can miss the initial Animated.View onLayout that
+    // normally triggers onOpenDialog, leaving the sheet translated off-screen
+    // (Appium: node exists but displayed=false). Open once on mount if layout
+    // has not already done so.
+    (0, react_1.useEffect)(() => {
+        const frame = requestAnimationFrame(() => {
+            if (!isMounted.current) {
+                isMounted.current = true;
+                onOpenDialog();
+            }
+        });
+        return () => cancelAnimationFrame(frame);
+    }, []);
     const updateSheetHeight = (e) => {
         const { height } = e.nativeEvent.layout;
         bottomOfDialogYValue.value = height;
@@ -216,7 +236,7 @@ exports.BottomSheetDialog = (0, react_1.forwardRef)(({ children, isFullscreen =
     }));
     return (<react_native_1.KeyboardAvoidingView style={tw.style('absolute bottom-0 inset-x-0')} behavior={react_native_1.Platform.OS === 'ios' ? 'padding' : undefined} keyboardVerticalOffset={react_native_1.Platform.OS === 'ios' ? -screenBottomPadding : frameY} enabled={keyboardAvoidingViewEnabled} {...props}>
         <react_native_gesture_handler_1.GestureDetector gesture={gestureHandler}>
-          <react_native_reanimated_1.default.View onLayout={updateSheetHeight} style={combinedSheetStyle}>
+          <react_native_reanimated_1.default.View collapsable={false} onLayout={updateSheetHeight} style={combinedSheetStyle}>
             {isInteractable && (<react_native_1.View style={tw.style('self-stretch items-center p-1')}>
                 <react_native_1.View style={tw.style('h-1 w-10 rounded-sm bg-border-muted')}/>
               </react_native_1.View>)}
diff --git a/dist/components/BottomSheetDialog/BottomSheetDialog.mjs b/dist/components/BottomSheetDialog/BottomSheetDialog.mjs
index d48eb56d611614affc8dd2bac497a498d1f88784..6863f5d5c27af64377c69e8b02f5113c65da8faf 100644
--- a/dist/components/BottomSheetDialog/BottomSheetDialog.mjs
+++ b/dist/components/BottomSheetDialog/BottomSheetDialog.mjs
@@ -154,6 +154,21 @@ export const BottomSheetDialog = forwardRef(({ children, isFullscreen = false, i
     // Disable for now since network switches causes the screen to hang with this on.
     // LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);
     onDebouncedCloseDialog.cancel(), [children, onDebouncedCloseDialog]);
+    // Reanimated 4 / Fabric can miss the initial Animated.View onLayout that
+    // normally triggers onOpenDialog, leaving the sheet translated off-screen
+    // (Appium: node exists but displayed=false). Open once on mount if layout
+    // has not already done so.
+    useEffect(() => {
+        const frame = requestAnimationFrame(() => {
+            if (!isMounted.current) {
+                isMounted.current = true;
+                onOpenDialog();
+            }
+        });
+        return () => cancelAnimationFrame(frame);
+        // Mount-once fallback; onOpenDialog closes over stable shared values.
+        // eslint-disable-next-line react-hooks/exhaustive-deps
+    }, []);
     const updateSheetHeight = (e) => {
         const { height } = e.nativeEvent.layout;
         bottomOfDialogYValue.value = height;
@@ -198,7 +213,7 @@ export const BottomSheetDialog = forwardRef(({ children, isFullscreen = false, i
     }));
     return (<KeyboardAvoidingView style={tw.style('absolute bottom-0 inset-x-0')} behavior={Platform.OS === 'ios' ? 'padding' : undefined} keyboardVerticalOffset={Platform.OS === 'ios' ? -screenBottomPadding : frameY} enabled={keyboardAvoidingViewEnabled} {...props}>
         <GestureDetector gesture={gestureHandler}>
-          <Animated.View onLayout={updateSheetHeight} style={combinedSheetStyle}>
+          <Animated.View collapsable={false} onLayout={updateSheetHeight} style={combinedSheetStyle}>
             {isInteractable && (<View style={tw.style('self-stretch items-center p-1')}>
                 <View style={tw.style('h-1 w-10 rounded-sm bg-border-muted')}/>
               </View>)}
diff --git a/dist/components/Toast/Toaster.cjs b/dist/components/Toast/Toaster.cjs
index 82f3ad83e1b741131545591ca586dbf1d18d8b84..a45e78e7b310b00ace07b40cce28cd1126693345 100644
--- a/dist/components/Toast/Toaster.cjs
+++ b/dist/components/Toast/Toaster.cjs
@@ -32,6 +32,14 @@ const react_native_gesture_handler_1 = require("react-native-gesture-handler");
 const react_native_reanimated_1 = __importStar(require("react-native-reanimated"));
 const react_native_safe_area_context_1 = require("react-native-safe-area-context");
 const react_native_worklets_1 = require("react-native-worklets");
+// MM patch: capture the bare function, not the module namespace. The Worklets
+// babel plugin otherwise captures `react_native_worklets_1` (which contains the
+// WorkletsModule NativeWorklets instance) into worklet closures, and worklets
+// >=0.10 throws "Cannot copy value of type `NativeWorklets`" when serializing
+// them — fatal on Android bridgeless (kills the React host at sheet/toast open).
+const scheduleOnRN = react_native_worklets_1.scheduleOnRN;
+const withSpring = react_native_reanimated_1.withSpring;
+const cancelAnimation = react_native_reanimated_1.cancelAnimation;
 // Internal dependencies.
 const Toast_1 = require("./Toast.cjs");
 const Toast_constants_1 = require("./Toast.constants.cjs");
@@ -98,9 +106,9 @@ const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref
         clearScheduledAutoDismiss();
         isDismissing.value = true;
         visibleAtRef.current = null;
-        translateYProgress.value = (0, react_native_reanimated_1.withSpring)(hiddenTranslateY.value, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
+        translateYProgress.value = withSpring(hiddenTranslateY.value, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
             if (finished) {
-                (0, react_native_worklets_1.scheduleOnRN)(resetState);
+                scheduleOnRN(resetState);
             }
         });
     };
@@ -143,7 +151,7 @@ const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref
             // when the replacement is persistent (hasNoTimeout). Otherwise the old
             // timer can fire and dismiss the new toast.
             clearScheduledAutoDismiss();
-            (0, react_native_reanimated_1.cancelAnimation)(translateYProgress);
+            cancelAnimation(translateYProgress);
             timeoutDuration = 100;
             animationStartedRef.current = false;
             visibleAtRef.current = null;
@@ -195,11 +203,11 @@ const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref
         const springBackAfterSwipe = () => {
             'worklet';
             const generationAtSpringStart = toastGeneration.value;
-            translateYProgress.value = (0, react_native_reanimated_1.withSpring)(visibleTranslateY.value, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
+            translateYProgress.value = withSpring(visibleTranslateY.value, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
                 // A new pan cancels this spring via cancelAnimation; only resume
                 // auto-dismiss when the spring-back completed naturally.
                 if (finished) {
-                    (0, react_native_worklets_1.scheduleOnRN)(resumeAutoDismissFromSwipe, generationAtSpringStart);
+                    scheduleOnRN(resumeAutoDismissFromSwipe, generationAtSpringStart);
                 }
             });
         };
@@ -213,8 +221,8 @@ const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref
                 return;
             }
             isSwipeActive.value = true;
-            (0, react_native_worklets_1.scheduleOnRN)(clearScheduledAutoDismissFromSwipe);
-            (0, react_native_reanimated_1.cancelAnimation)(translateYProgress);
+            scheduleOnRN(clearScheduledAutoDismissFromSwipe);
+            cancelAnimation(translateYProgress);
             gestureStartY.value = translateYProgress.value;
         })
             .onUpdate((event) => {
@@ -243,7 +251,7 @@ const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref
             const shouldDismiss = hasReachedDismissOffset ||
                 (hasReachedSwipeThreshold && isQuickDismissing);
             if (shouldDismiss) {
-                (0, react_native_worklets_1.scheduleOnRN)(dismissToastFromSwipe);
+                scheduleOnRN(dismissToastFromSwipe);
                 return;
             }
             springBackAfterSwipe();
@@ -308,12 +316,12 @@ const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref
         animationStartedRef.current = true;
         translateYProgress.value = nextHiddenTranslateY;
         if (toastOptions.hasNoTimeout) {
-            translateYProgress.value = (0, react_native_reanimated_1.withSpring)(nextVisibleTranslateY, Toast_constants_1.TOAST_SPRING_CONFIG);
+            translateYProgress.value = withSpring(nextVisibleTranslateY, Toast_constants_1.TOAST_SPRING_CONFIG);
         }
         else {
-            translateYProgress.value = (0, react_native_reanimated_1.withSpring)(nextVisibleTranslateY, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
+            translateYProgress.value = withSpring(nextVisibleTranslateY, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
                 if (finished) {
-                    (0, react_native_worklets_1.scheduleOnRN)(beginAutoDismiss);
+                    scheduleOnRN(beginAutoDismiss);
                 }
             });
         }
diff --git a/dist/components/Toast/Toaster.cjs.orig b/dist/components/Toast/Toaster.cjs.orig
new file mode 100644
index 0000000000000000000000000000000000000000..82f3ad83e1b741131545591ca586dbf1d18d8b84
--- /dev/null
+++ b/dist/components/Toast/Toaster.cjs.orig
@@ -0,0 +1,335 @@
+"use strict";
+var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
+    if (k2 === undefined) k2 = k;
+    var desc = Object.getOwnPropertyDescriptor(m, k);
+    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
+      desc = { enumerable: true, get: function() { return m[k]; } };
+    }
+    Object.defineProperty(o, k2, desc);
+}) : (function(o, m, k, k2) {
+    if (k2 === undefined) k2 = k;
+    o[k2] = m[k];
+}));
+var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
+    Object.defineProperty(o, "default", { enumerable: true, value: v });
+}) : function(o, v) {
+    o["default"] = v;
+});
+var __importStar = (this && this.__importStar) || function (mod) {
+    if (mod && mod.__esModule) return mod;
+    var result = {};
+    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
+    __setModuleDefault(result, mod);
+    return result;
+};
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.toast = exports.Toaster = void 0;
+// Third party dependencies.
+const design_system_twrnc_preset_1 = require("@metamask/design-system-twrnc-preset");
+const react_1 = __importStar(require("react"));
+const react_native_1 = require("react-native");
+const react_native_gesture_handler_1 = require("react-native-gesture-handler");
+const react_native_reanimated_1 = __importStar(require("react-native-reanimated"));
+const react_native_safe_area_context_1 = require("react-native-safe-area-context");
+const react_native_worklets_1 = require("react-native-worklets");
+// Internal dependencies.
+const Toast_1 = require("./Toast.cjs");
+const Toast_constants_1 = require("./Toast.constants.cjs");
+const screenHeight = react_native_1.Dimensions.get('window').height;
+let registeredRef = null;
+const assertRegisteredRef = (method) => {
+    if (!registeredRef?.current) {
+        const invocation = method === 'toast' ? 'toast()' : `toast.${method}()`;
+        throw new Error(`${invocation} called before <Toaster /> mounted. Render <Toaster /> once at the root of your app.`);
+    }
+    return registeredRef.current;
+};
+const getToastProps = ({ hasNoTimeout: _hasNoTimeout, onClose: _onClose, topOffset: _topOffset, twClassName: _twClassName, showCloseButton: _showCloseButton, ...toastProps }) => toastProps;
+const getHiddenTranslateY = (height, offset) => -(height + offset);
+const ToasterComponent = (0, react_1.forwardRef)(({ twClassName, ...props }, ref) => {
+    const tw = (0, design_system_twrnc_preset_1.useTailwind)();
+    const [toastOptions, setToastOptions] = (0, react_1.useState)(undefined);
+    const replacementTimerRef = (0, react_1.useRef)(null);
+    const autoDismissTimeoutRef = (0, react_1.useRef)(null);
+    const animationStartedRef = (0, react_1.useRef)(false);
+    const visibleAtRef = (0, react_1.useRef)(null);
+    const hasNoTimeoutRef = (0, react_1.useRef)(false);
+    // Invalidates queued spring-back resumes after replace/dismiss. A completed
+    // spring cannot be cancelled, so scheduleOnRN(resume) can still run later.
+    const toastGenerationRef = (0, react_1.useRef)(0);
+    const { top: topInset } = (0, react_native_safe_area_context_1.useSafeAreaInsets)();
+    const toastHeight = (0, react_native_reanimated_1.useSharedValue)(screenHeight);
+    const hiddenTranslateY = (0, react_native_reanimated_1.useSharedValue)(-screenHeight);
+    const visibleTranslateY = (0, react_native_reanimated_1.useSharedValue)(0);
+    const gestureStartY = (0, react_native_reanimated_1.useSharedValue)(0);
+    const translateYProgress = (0, react_native_reanimated_1.useSharedValue)(-screenHeight);
+    const isDismissing = (0, react_native_reanimated_1.useSharedValue)(false);
+    // True after onStart until onEnd/onFinalize recovers. Used so a failed pan
+    // (e.g. failOffsetX after activation) still springs back and resumes dismiss.
+    const isSwipeActive = (0, react_native_reanimated_1.useSharedValue)(false);
+    const toastGeneration = (0, react_native_reanimated_1.useSharedValue)(0);
+    const topOffset = toastOptions?.topOffset ?? 0;
+    hasNoTimeoutRef.current = Boolean(toastOptions?.hasNoTimeout);
+    const animatedStyle = (0, react_native_reanimated_1.useAnimatedStyle)(() => ({
+        transform: [{ translateY: translateYProgress.value + topOffset }],
+    }));
+    const baseStyle = (0, react_1.useMemo)(() => [tw.style('absolute left-4 right-4 top-0'), animatedStyle], [tw, animatedStyle]);
+    const innerRef = (0, react_1.useRef)(null);
+    const clearScheduledAutoDismiss = () => {
+        if (autoDismissTimeoutRef.current !== null) {
+            clearTimeout(autoDismissTimeoutRef.current);
+            autoDismissTimeoutRef.current = null;
+        }
+    };
+    const bumpToastGeneration = () => {
+        toastGenerationRef.current += 1;
+        toastGeneration.value = toastGenerationRef.current;
+    };
+    const resetState = () => {
+        animationStartedRef.current = false;
+        visibleAtRef.current = null;
+        isDismissing.value = false;
+        isSwipeActive.value = false;
+        clearScheduledAutoDismiss();
+        setToastOptions(undefined);
+    };
+    const startDismissAnimation = () => {
+        bumpToastGeneration();
+        clearScheduledAutoDismiss();
+        isDismissing.value = true;
+        visibleAtRef.current = null;
+        translateYProgress.value = (0, react_native_reanimated_1.withSpring)(hiddenTranslateY.value, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
+            if (finished) {
+                (0, react_native_worklets_1.scheduleOnRN)(resetState);
+            }
+        });
+    };
+    const scheduleAutoDismiss = (delayMs) => {
+        clearScheduledAutoDismiss();
+        autoDismissTimeoutRef.current = setTimeout(() => {
+            autoDismissTimeoutRef.current = null;
+            startDismissAnimation();
+        }, delayMs);
+    };
+    const beginAutoDismiss = () => {
+        visibleAtRef.current = Date.now();
+        scheduleAutoDismiss(Toast_constants_1.TOAST_VISIBILITY_DURATION);
+    };
+    const resumeAutoDismissAfterSwipe = () => {
+        if (hasNoTimeoutRef.current || isDismissing.value) {
+            return;
+        }
+        // Entrance was interrupted before auto-dismiss started — start a full timer.
+        if (visibleAtRef.current === null) {
+            beginAutoDismiss();
+            return;
+        }
+        const elapsed = Date.now() - visibleAtRef.current;
+        if (elapsed >= Toast_constants_1.TOAST_VISIBILITY_DURATION) {
+            startDismissAnimation();
+            return;
+        }
+        scheduleAutoDismiss(Toast_constants_1.TOAST_VISIBILITY_DURATION - elapsed);
+    };
+    const showToast = (options) => {
+        bumpToastGeneration();
+        let timeoutDuration = 0;
+        const normalizedOptions = {
+            hasNoTimeout: false,
+            ...options,
+        };
+        if (toastOptions) {
+            // Always clear any pending auto-dismiss from the previous toast, including
+            // when the replacement is persistent (hasNoTimeout). Otherwise the old
+            // timer can fire and dismiss the new toast.
+            clearScheduledAutoDismiss();
+            (0, react_native_reanimated_1.cancelAnimation)(translateYProgress);
+            timeoutDuration = 100;
+            animationStartedRef.current = false;
+            visibleAtRef.current = null;
+            isDismissing.value = false;
+            isSwipeActive.value = false;
+            setToastOptions(undefined);
+        }
+        if (replacementTimerRef.current !== null) {
+            clearTimeout(replacementTimerRef.current);
+        }
+        replacementTimerRef.current = setTimeout(() => {
+            replacementTimerRef.current = null;
+            setToastOptions(normalizedOptions);
+        }, timeoutDuration);
+    };
+    const closeToast = () => {
+        if (replacementTimerRef.current !== null) {
+            clearTimeout(replacementTimerRef.current);
+            replacementTimerRef.current = null;
+        }
+        startDismissAnimation();
+    };
+    const closeToastRef = (0, react_1.useRef)(closeToast);
+    const resumeAutoDismissAfterSwipeRef = (0, react_1.useRef)(resumeAutoDismissAfterSwipe);
+    const clearScheduledAutoDismissRef = (0, react_1.useRef)(clearScheduledAutoDismiss);
+    closeToastRef.current = closeToast;
+    resumeAutoDismissAfterSwipeRef.current = resumeAutoDismissAfterSwipe;
+    clearScheduledAutoDismissRef.current = clearScheduledAutoDismiss;
+    const dismissToastFromSwipe = () => {
+        closeToastRef.current();
+    };
+    const resumeAutoDismissFromSwipe = (generation) => {
+        // Ignore resumes from a toast that was replaced or dismissed after the
+        // spring-back started (completed springs cannot be cancelled).
+        if (generation !== toastGenerationRef.current) {
+            return;
+        }
+        resumeAutoDismissAfterSwipeRef.current();
+    };
+    const clearScheduledAutoDismissFromSwipe = () => {
+        clearScheduledAutoDismissRef.current();
+    };
+    const swipeGesture = (0, react_1.useMemo)(() => {
+        // These gesture callbacks need explicit 'worklet' directives because this
+        // package ships a pre-built dist compiled by ts-bridge (tsc), which emits the
+        // gesture chain as a namespaced call (react_native_gesture_handler_1.Gesture).
+        // The consumer's Reanimated/Worklets Babel plugin does run over dist, but its
+        // gesture auto-detection doesn't recognize that compiled namespaced form.
+        const springBackAfterSwipe = () => {
+            'worklet';
+            const generationAtSpringStart = toastGeneration.value;
+            translateYProgress.value = (0, react_native_reanimated_1.withSpring)(visibleTranslateY.value, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
+                // A new pan cancels this spring via cancelAnimation; only resume
+                // auto-dismiss when the spring-back completed naturally.
+                if (finished) {
+                    (0, react_native_worklets_1.scheduleOnRN)(resumeAutoDismissFromSwipe, generationAtSpringStart);
+                }
+            });
+        };
+        return react_native_gesture_handler_1.Gesture.Pan()
+            .activeOffsetY(Toast_constants_1.TOAST_SWIPE_ACTIVE_OFFSET_Y)
+            .failOffsetX([-Toast_constants_1.TOAST_SWIPE_FAIL_OFFSET_X, Toast_constants_1.TOAST_SWIPE_FAIL_OFFSET_X])
+            .onStart(() => {
+            'worklet';
+            // Don't interrupt an in-progress dismiss animation.
+            if (isDismissing.value) {
+                return;
+            }
+            isSwipeActive.value = true;
+            (0, react_native_worklets_1.scheduleOnRN)(clearScheduledAutoDismissFromSwipe);
+            (0, react_native_reanimated_1.cancelAnimation)(translateYProgress);
+            gestureStartY.value = translateYProgress.value;
+        })
+            .onUpdate((event) => {
+            'worklet';
+            if (isDismissing.value || !isSwipeActive.value) {
+                return;
+            }
+            const nextTranslateY = gestureStartY.value + event.translationY;
+            // Toast sits at the top; only allow dragging upward (more negative).
+            translateYProgress.value = Math.min(nextTranslateY, visibleTranslateY.value);
+        })
+            .onEnd((event) => {
+            'worklet';
+            if (!isSwipeActive.value) {
+                return;
+            }
+            isSwipeActive.value = false;
+            if (isDismissing.value) {
+                return;
+            }
+            const { translationY, velocityY } = event;
+            const dismissDistance = Math.max(toastHeight.value * Toast_constants_1.TOAST_DISMISS_DISTANCE_THRESHOLD, Toast_constants_1.TOAST_DISMISS_MIN_DISTANCE);
+            const hasReachedDismissOffset = translationY <= -dismissDistance;
+            const hasReachedSwipeThreshold = Math.abs(velocityY) > Toast_constants_1.TOAST_DISMISS_VELOCITY_THRESHOLD;
+            const isQuickDismissing = velocityY < 0;
+            const shouldDismiss = hasReachedDismissOffset ||
+                (hasReachedSwipeThreshold && isQuickDismissing);
+            if (shouldDismiss) {
+                (0, react_native_worklets_1.scheduleOnRN)(dismissToastFromSwipe);
+                return;
+            }
+            springBackAfterSwipe();
+        })
+            .onFinalize(() => {
+            'worklet';
+            // onEnd is skipped when the pan fails/cancels after activation
+            // (e.g. horizontal travel past failOffsetX). Recover position and
+            // auto-dismiss so the toast is not left stuck mid-offset.
+            if (!isSwipeActive.value) {
+                return;
+            }
+            // Clear before the dismiss check — same order as onEnd — so a pan
+            // cancelled mid-dismiss cannot leave isSwipeActive stuck true for a
+            // later toast (resetState alone is not enough if finalize races it).
+            isSwipeActive.value = false;
+            if (isDismissing.value) {
+                return;
+            }
+            springBackAfterSwipe();
+        });
+        // Shared values and swipe JS wrappers are stable for the component lifetime.
+    }, []);
+    innerRef.current = {
+        closeToast,
+        showToast,
+    };
+    (0, react_1.useImperativeHandle)(ref, () => innerRef.current);
+    (0, react_1.useLayoutEffect)(() => {
+        registeredRef = innerRef;
+        return () => {
+            if (registeredRef === innerRef) {
+                registeredRef = null;
+            }
+        };
+    }, []);
+    if (!toastOptions) {
+        return null;
+    }
+    const { onClose: toastOnClose, showCloseButton = true, twClassName: toastTwClassName, } = toastOptions;
+    const toastProps = getToastProps(toastOptions);
+    const { actionButtonLabel, actionButtonOnPress, ...restToastProps } = toastProps;
+    const toastTwClassNames = [twClassName, toastTwClassName]
+        .filter(Boolean)
+        .join(' ');
+    const handleClose = showCloseButton
+        ? () => {
+            closeToast();
+            toastOnClose?.();
+        }
+        : undefined;
+    const onAnimatedViewLayout = (e) => {
+        const { height } = e.nativeEvent.layout;
+        const nextHiddenTranslateY = getHiddenTranslateY(height, topOffset);
+        const nextVisibleTranslateY = topInset + Toast_constants_1.TOAST_TOP_PADDING;
+        hiddenTranslateY.value = nextHiddenTranslateY;
+        visibleTranslateY.value = nextVisibleTranslateY;
+        toastHeight.value = height;
+        if (animationStartedRef.current) {
+            return;
+        }
+        animationStartedRef.current = true;
+        translateYProgress.value = nextHiddenTranslateY;
+        if (toastOptions.hasNoTimeout) {
+            translateYProgress.value = (0, react_native_reanimated_1.withSpring)(nextVisibleTranslateY, Toast_constants_1.TOAST_SPRING_CONFIG);
+        }
+        else {
+            translateYProgress.value = (0, react_native_reanimated_1.withSpring)(nextVisibleTranslateY, Toast_constants_1.TOAST_SPRING_CONFIG, (finished) => {
+                if (finished) {
+                    (0, react_native_worklets_1.scheduleOnRN)(beginAutoDismiss);
+                }
+            });
+        }
+    };
+    return (<react_native_gesture_handler_1.GestureDetector gesture={swipeGesture}>
+        <react_native_reanimated_1.default.View onLayout={onAnimatedViewLayout} style={baseStyle} {...props}>
+          {actionButtonLabel && actionButtonOnPress ? (<Toast_1.Toast {...toastProps} actionButtonLabel={actionButtonLabel} actionButtonOnPress={actionButtonOnPress} onClose={handleClose} twClassName={toastTwClassNames}/>) : (<Toast_1.Toast {...restToastProps} onClose={handleClose} twClassName={toastTwClassNames}/>)}
+        </react_native_reanimated_1.default.View>
+      </react_native_gesture_handler_1.GestureDetector>);
+});
+ToasterComponent.displayName = 'Toaster';
+exports.Toaster = ToasterComponent;
+exports.toast = ((options) => {
+    assertRegisteredRef('toast').showToast(options);
+});
+exports.toast.dismiss = () => {
+    assertRegisteredRef('dismiss').closeToast();
+};
+//# sourceMappingURL=Toaster.cjs.map
\ No newline at end of file
