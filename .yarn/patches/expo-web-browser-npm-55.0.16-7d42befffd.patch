diff --git a/ios/WebAuthSession.swift b/ios/WebAuthSession.swift
index acd6fb25782964cac394dc5e2196ae957c8c956b..475cb7f65c1dd308a8178d8d65039309f66deb43 100644
--- a/ios/WebAuthSession.swift
+++ b/ios/WebAuthSession.swift
@@ -43,9 +43,11 @@ final internal class WebAuthSession {
        !host.isEmpty {
       // Use the new callback API for HTTPS universal links
       // Pass an empty string for the path to match any path under the host if no specific path is provided
+      let rawPath = redirectUrl.path
+      let path = (rawPath.isEmpty || rawPath == "/") ? "" : rawPath
       self.authSession = ASWebAuthenticationSession(
         url: authUrl,
-        callback: .https(host: host, path: redirectUrl.path),
+        callback: .https(host: host, path: path),
         completionHandler: completionHandler
       )
     } else {
