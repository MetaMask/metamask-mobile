diff --git a/dist/AssetsContractController.cjs b/dist/AssetsContractController.cjs
index bbdc0ed81a0e5c68caacf4c1d2a3276ae765e398..1444bc7c2d6f790dd03617d83d44506517c6a8b2 100644
--- a/dist/AssetsContractController.cjs
+++ b/dist/AssetsContractController.cjs
@@ -50,6 +50,7 @@ exports.SINGLE_CALL_BALANCES_ADDRESS_BY_CHAINID = {
     [assetsUtil_1.SupportedTokenDetectionNetworks.PolygonZkevm]: '0x6aa75276052d96696134252587894ef5ffa520af',
     [assetsUtil_1.SupportedTokenDetectionNetworks.Moonbeam]: '0x6aa75276052d96696134252587894ef5ffa520af',
     [assetsUtil_1.SupportedTokenDetectionNetworks.Moonriver]: '0x6aa75276052d96696134252587894ef5ffa520af',
+    [assetsUtil_1.SupportedTokenDetectionNetworks.Robinhood]: '0x1C0b2428d5C520EF51310dd1f93fBA6B58b47dA6',
 };
 exports.STAKING_CONTRACT_ADDRESS_BY_CHAINID = {
     [assetsUtil_1.SupportedStakedBalanceNetworks.Mainnet]: '0x4fef9d741011476750a243ac70b9789a63dd47df',
diff --git a/dist/AssetsContractController.mjs b/dist/AssetsContractController.mjs
index 546c20346da1aa55b63372d7759fb766de138ac9..846016d0c8d837a1a6e7d84c26ca498434e33078 100644
--- a/dist/AssetsContractController.mjs
+++ b/dist/AssetsContractController.mjs
@@ -51,6 +51,7 @@ export const SINGLE_CALL_BALANCES_ADDRESS_BY_CHAINID = {
     [SupportedTokenDetectionNetworks.PolygonZkevm]: '0x6aa75276052d96696134252587894ef5ffa520af',
     [SupportedTokenDetectionNetworks.Moonbeam]: '0x6aa75276052d96696134252587894ef5ffa520af',
     [SupportedTokenDetectionNetworks.Moonriver]: '0x6aa75276052d96696134252587894ef5ffa520af',
+    [SupportedTokenDetectionNetworks.Robinhood]: '0x1C0b2428d5C520EF51310dd1f93fBA6B58b47dA6',
 };
 export const STAKING_CONTRACT_ADDRESS_BY_CHAINID = {
     [SupportedStakedBalanceNetworks.Mainnet]: '0x4fef9d741011476750a243ac70b9789a63dd47df',
diff --git a/dist/assetsUtil.cjs b/dist/assetsUtil.cjs
index bc9ba89a8a78ee52259de01f859d77967b2dd308..ff1241de28e113da39528564bce74f82e31f0fce 100644
--- a/dist/assetsUtil.cjs
+++ b/dist/assetsUtil.cjs
@@ -137,6 +137,7 @@ var SupportedTokenDetectionNetworks;
     SupportedTokenDetectionNetworks["MonadMainnet"] = "0x8f";
     SupportedTokenDetectionNetworks["Hyperevm"] = "0x3e7";
     SupportedTokenDetectionNetworks["Arc"] = "0x13b2";
+    SupportedTokenDetectionNetworks["Robinhood"] = "0x1237";
 })(SupportedTokenDetectionNetworks || (exports.SupportedTokenDetectionNetworks = SupportedTokenDetectionNetworks = {}));
 /**
  * Networks where staked balance is supported - Values are in hex format
diff --git a/dist/assetsUtil.mjs b/dist/assetsUtil.mjs
index 8325a1b97fdcf617882621f09a3d62e34a19173e..e0d3ffefbaccff24f2fe6facb2da55a6e9f341bd 100644
--- a/dist/assetsUtil.mjs
+++ b/dist/assetsUtil.mjs
@@ -134,6 +134,7 @@ export var SupportedTokenDetectionNetworks;
     SupportedTokenDetectionNetworks["MonadMainnet"] = "0x8f";
     SupportedTokenDetectionNetworks["Hyperevm"] = "0x3e7";
     SupportedTokenDetectionNetworks["Arc"] = "0x13b2";
+    SupportedTokenDetectionNetworks["Robinhood"] = "0x1237";
 })(SupportedTokenDetectionNetworks || (SupportedTokenDetectionNetworks = {}));
 /**
  * Networks where staked balance is supported - Values are in hex format
diff --git a/dist/token-service.cjs b/dist/token-service.cjs
index 823b779a6998ce50e7cecbfd0e1d05275fba84bd..05f2eea72f8755abb9095560a446bc286c4288a1 100644
--- a/dist/token-service.cjs
+++ b/dist/token-service.cjs
@@ -1,22 +1,101 @@
 "use strict";
 Object.defineProperty(exports, "__esModule", { value: true });
-exports.fetchTokenMetadata = exports.fetchRwas = exports.fetchTokenAssets = exports.getTrendingTokens = exports.searchTokens = exports.fetchTokenListByChainId = exports.TOKEN_METADATA_NO_SUPPORT_ERROR = exports.TOKEN_END_POINT_API = void 0;
+exports.fetchTokenMetadata = exports.fetchRwas = exports.fetchTokenAssets = exports.getTrendingTokens = exports.searchTokens = exports.fetchTokenListByChainId = exports.resetSuggestedOccurrenceFloorsCacheForTesting = exports.TOKEN_METADATA_NO_SUPPORT_ERROR = exports.TOKEN_END_POINT_API = void 0;
 const controller_utils_1 = require("@metamask/controller-utils");
 const assetsUtil_1 = require("./assetsUtil.cjs");
 exports.TOKEN_END_POINT_API = 'https://token.api.cx.metamask.io';
 exports.TOKEN_METADATA_NO_SUPPORT_ERROR = 'TokenService Error: Network does not support fetchTokenMetadata';
+/**
+ * Per-chain suggested occurrence floors endpoint. Used as the `occurrenceFloor`
+ * query param when fetching token lists (aligned with assets-controller
+ * TokenDataSource / TokensApiClient).
+ */
+const SUGGESTED_OCCURRENCE_FLOORS_URL = `${exports.TOKEN_END_POINT_API}/v1/suggestedOccurrenceFloors`;
+/**
+ * Fallback `occurrenceFloor` when `/v1/suggestedOccurrenceFloors` has no entry
+ * for the chain, or the floors request fails.
+ */
+const DEFAULT_OCCURRENCE_FLOOR = 3;
+/** How long to keep suggested occurrence floors cached (1 hour). */
+const SUGGESTED_OCCURRENCE_FLOORS_CACHE_TTL_MS = 60 * 60000;
+/** Decimal chain ID → suggested occurrence floor. */
+let suggestedOccurrenceFloorsCache;
+/** Timestamp of the last successful (or failed-open) floors fetch. */
+let suggestedOccurrenceFloorsCachedAt = 0;
+/** In-flight floors request shared across concurrent callers. */
+let suggestedOccurrenceFloorsRefreshPromise;
+/**
+ * Clears the suggested occurrence floors cache. Exported for unit tests only.
+ */
+function resetSuggestedOccurrenceFloorsCacheForTesting() {
+    suggestedOccurrenceFloorsCache = undefined;
+    suggestedOccurrenceFloorsCachedAt = 0;
+    suggestedOccurrenceFloorsRefreshPromise = undefined;
+}
+exports.resetSuggestedOccurrenceFloorsCacheForTesting = resetSuggestedOccurrenceFloorsCacheForTesting;
+/**
+ * Fetch `/v1/suggestedOccurrenceFloors` with a 1h in-memory cache. Failures
+ * return an empty map so callers fall back to {@link DEFAULT_OCCURRENCE_FLOOR}.
+ *
+ * @returns Map of decimal chain ID → suggested occurrence floor.
+ */
+async function getSuggestedOccurrenceFloors() {
+    const now = Date.now();
+    if (suggestedOccurrenceFloorsCache !== undefined &&
+        now - suggestedOccurrenceFloorsCachedAt <
+            SUGGESTED_OCCURRENCE_FLOORS_CACHE_TTL_MS) {
+        return suggestedOccurrenceFloorsCache;
+    }
+    if (suggestedOccurrenceFloorsRefreshPromise !== undefined) {
+        return suggestedOccurrenceFloorsRefreshPromise;
+    }
+    suggestedOccurrenceFloorsRefreshPromise = (async () => {
+        try {
+            const response = await fetch(SUGGESTED_OCCURRENCE_FLOORS_URL);
+            if (response.ok) {
+                const data = (await response.json());
+                suggestedOccurrenceFloorsCache =
+                    data && typeof data === 'object' && !Array.isArray(data)
+                        ? data
+                        : {};
+            }
+            else {
+                suggestedOccurrenceFloorsCache = {};
+            }
+        }
+        catch {
+            suggestedOccurrenceFloorsCache = {};
+        }
+        finally {
+            suggestedOccurrenceFloorsCachedAt = Date.now();
+            suggestedOccurrenceFloorsRefreshPromise = undefined;
+        }
+        return suggestedOccurrenceFloorsCache ?? {};
+    })();
+    return suggestedOccurrenceFloorsRefreshPromise;
+}
+/**
+ * Resolve the `occurrenceFloor` query param for a chain from Token API
+ * `/v1/suggestedOccurrenceFloors`. Falls back to
+ * {@link DEFAULT_OCCURRENCE_FLOOR} when the chain is missing or the request
+ * fails.
+ *
+ * @param chainId - Hex chain ID.
+ * @returns Occurrence floor to send to `/tokens/{chainId}`.
+ */
+async function getOccurrenceFloor(chainId) {
+    const floors = await getSuggestedOccurrenceFloors();
+    const decimalChainId = String((0, controller_utils_1.convertHexToDecimal)(chainId));
+    return floors[decimalChainId] ?? DEFAULT_OCCURRENCE_FLOOR;
+}
 /**
  * Get the tokens URL for a specific network.
  *
  * @param chainId - The chain ID of the network the tokens requested are on.
  * @returns The tokens URL.
  */
-function getTokensURL(chainId) {
-    const occurrenceFloor = chainId === controller_utils_1.ChainId['linea-mainnet'] ||
-        chainId === controller_utils_1.ChainId['megaeth-mainnet'] ||
-        chainId === '0x1079' // Tempo Mainnet
-        ? 1
-        : 3;
+async function getTokensURL(chainId) {
+    const occurrenceFloor = await getOccurrenceFloor(chainId);
     return `${exports.TOKEN_END_POINT_API}/tokens/${(0, controller_utils_1.convertHexToDecimal)(chainId)}?occurrenceFloor=${occurrenceFloor}&includeNativeAssets=false&includeTokenFees=false&includeAssetType=false&includeERC20Permit=false&includeStorage=false&includeRwaData=true`;
 }
 /**
@@ -170,7 +249,7 @@ const defaultTimeout = tenSecondsInMilliseconds;
  * @returns The token list, or `undefined` if the request was cancelled.
  */
 async function fetchTokenListByChainId(chainId, abortSignal, { timeout = defaultTimeout } = {}) {
-    const tokenURL = getTokensURL(chainId);
+    const tokenURL = await getTokensURL(chainId);
     const response = await queryApi(tokenURL, abortSignal, timeout);
     if (response) {
         const result = await parseJsonResponse(response);
diff --git a/dist/token-service.d.cts b/dist/token-service.d.cts
index 37d38ebb0d531d552470633646d4176e8546b25f..ddccfcf431844f8ea2c8eedd5faffb55398a797f 100644
--- a/dist/token-service.d.cts
+++ b/dist/token-service.d.cts
@@ -1,6 +1,10 @@
 import type { CaipAssetType, CaipChainId, Hex } from "@metamask/utils";
 export declare const TOKEN_END_POINT_API = "https://token.api.cx.metamask.io";
 export declare const TOKEN_METADATA_NO_SUPPORT_ERROR = "TokenService Error: Network does not support fetchTokenMetadata";
+/**
+ * Clears the suggested occurrence floors cache. Exported for unit tests only.
+ */
+export declare function resetSuggestedOccurrenceFloorsCacheForTesting(): void;
 /**
  * The sort by field for trending tokens.
  */
diff --git a/dist/token-service.d.mts b/dist/token-service.d.mts
index 8b7375fb4806e0b69274bee5c00a53e0ac749cda..3c4746872b3fb328bfa955fce9f8843f96a62da6 100644
--- a/dist/token-service.d.mts
+++ b/dist/token-service.d.mts
@@ -1,6 +1,10 @@
 import type { CaipAssetType, CaipChainId, Hex } from "@metamask/utils";
 export declare const TOKEN_END_POINT_API = "https://token.api.cx.metamask.io";
 export declare const TOKEN_METADATA_NO_SUPPORT_ERROR = "TokenService Error: Network does not support fetchTokenMetadata";
+/**
+ * Clears the suggested occurrence floors cache. Exported for unit tests only.
+ */
+export declare function resetSuggestedOccurrenceFloorsCacheForTesting(): void;
 /**
  * The sort by field for trending tokens.
  */
diff --git a/dist/token-service.mjs b/dist/token-service.mjs
index a3d48673471625b1ce0d8357c67ccff22c701ca1..66df4f927740c7cec1f785810394a4ac2ddee8a8 100644
--- a/dist/token-service.mjs
+++ b/dist/token-service.mjs
@@ -2,18 +2,96 @@ import { ChainId, convertHexToDecimal, handleFetch, timeoutFetch } from "@metama
 import { isTokenListSupportedForNetwork } from "./assetsUtil.mjs";
 export const TOKEN_END_POINT_API = 'https://token.api.cx.metamask.io';
 export const TOKEN_METADATA_NO_SUPPORT_ERROR = 'TokenService Error: Network does not support fetchTokenMetadata';
+/**
+ * Per-chain suggested occurrence floors endpoint. Used as the `occurrenceFloor`
+ * query param when fetching token lists (aligned with assets-controller
+ * TokenDataSource / TokensApiClient).
+ */
+const SUGGESTED_OCCURRENCE_FLOORS_URL = `${TOKEN_END_POINT_API}/v1/suggestedOccurrenceFloors`;
+/**
+ * Fallback `occurrenceFloor` when `/v1/suggestedOccurrenceFloors` has no entry
+ * for the chain, or the floors request fails.
+ */
+const DEFAULT_OCCURRENCE_FLOOR = 3;
+/** How long to keep suggested occurrence floors cached (1 hour). */
+const SUGGESTED_OCCURRENCE_FLOORS_CACHE_TTL_MS = 60 * 60000;
+/** Decimal chain ID → suggested occurrence floor. */
+let suggestedOccurrenceFloorsCache;
+/** Timestamp of the last successful (or failed-open) floors fetch. */
+let suggestedOccurrenceFloorsCachedAt = 0;
+/** In-flight floors request shared across concurrent callers. */
+let suggestedOccurrenceFloorsRefreshPromise;
+/**
+ * Clears the suggested occurrence floors cache. Exported for unit tests only.
+ */
+export function resetSuggestedOccurrenceFloorsCacheForTesting() {
+    suggestedOccurrenceFloorsCache = undefined;
+    suggestedOccurrenceFloorsCachedAt = 0;
+    suggestedOccurrenceFloorsRefreshPromise = undefined;
+}
+/**
+ * Fetch `/v1/suggestedOccurrenceFloors` with a 1h in-memory cache. Failures
+ * return an empty map so callers fall back to {@link DEFAULT_OCCURRENCE_FLOOR}.
+ *
+ * @returns Map of decimal chain ID → suggested occurrence floor.
+ */
+async function getSuggestedOccurrenceFloors() {
+    const now = Date.now();
+    if (suggestedOccurrenceFloorsCache !== undefined &&
+        now - suggestedOccurrenceFloorsCachedAt <
+            SUGGESTED_OCCURRENCE_FLOORS_CACHE_TTL_MS) {
+        return suggestedOccurrenceFloorsCache;
+    }
+    if (suggestedOccurrenceFloorsRefreshPromise !== undefined) {
+        return suggestedOccurrenceFloorsRefreshPromise;
+    }
+    suggestedOccurrenceFloorsRefreshPromise = (async () => {
+        try {
+            const response = await fetch(SUGGESTED_OCCURRENCE_FLOORS_URL);
+            if (response.ok) {
+                const data = (await response.json());
+                suggestedOccurrenceFloorsCache =
+                    data && typeof data === 'object' && !Array.isArray(data)
+                        ? data
+                        : {};
+            }
+            else {
+                suggestedOccurrenceFloorsCache = {};
+            }
+        }
+        catch {
+            suggestedOccurrenceFloorsCache = {};
+        }
+        finally {
+            suggestedOccurrenceFloorsCachedAt = Date.now();
+            suggestedOccurrenceFloorsRefreshPromise = undefined;
+        }
+        return suggestedOccurrenceFloorsCache ?? {};
+    })();
+    return suggestedOccurrenceFloorsRefreshPromise;
+}
+/**
+ * Resolve the `occurrenceFloor` query param for a chain from Token API
+ * `/v1/suggestedOccurrenceFloors`. Falls back to
+ * {@link DEFAULT_OCCURRENCE_FLOOR} when the chain is missing or the request
+ * fails.
+ *
+ * @param chainId - Hex chain ID.
+ * @returns Occurrence floor to send to `/tokens/{chainId}`.
+ */
+async function getOccurrenceFloor(chainId) {
+    const floors = await getSuggestedOccurrenceFloors();
+    const decimalChainId = String(convertHexToDecimal(chainId));
+    return floors[decimalChainId] ?? DEFAULT_OCCURRENCE_FLOOR;
+}
 /**
  * Get the tokens URL for a specific network.
  *
  * @param chainId - The chain ID of the network the tokens requested are on.
  * @returns The tokens URL.
  */
-function getTokensURL(chainId) {
-    const occurrenceFloor = chainId === ChainId['linea-mainnet'] ||
-        chainId === ChainId['megaeth-mainnet'] ||
-        chainId === '0x1079' // Tempo Mainnet
-        ? 1
-        : 3;
+async function getTokensURL(chainId) {
+    const occurrenceFloor = await getOccurrenceFloor(chainId);
     return `${TOKEN_END_POINT_API}/tokens/${convertHexToDecimal(chainId)}?occurrenceFloor=${occurrenceFloor}&includeNativeAssets=false&includeTokenFees=false&includeAssetType=false&includeERC20Permit=false&includeStorage=false&includeRwaData=true`;
 }
 /**
@@ -167,7 +245,7 @@ const defaultTimeout = tenSecondsInMilliseconds;
  * @returns The token list, or `undefined` if the request was cancelled.
  */
 export async function fetchTokenListByChainId(chainId, abortSignal, { timeout = defaultTimeout } = {}) {
-    const tokenURL = getTokensURL(chainId);
+    const tokenURL = await getTokensURL(chainId);
     const response = await queryApi(tokenURL, abortSignal, timeout);
     if (response) {
         const result = await parseJsonResponse(response);
diff --git a/dist/constants.cjs b/dist/constants.cjs
index 22d39c1fc6a9c921910841ec3bb90ce853f6c82b..57c56dfd88fb51fdee243469316fe9fdcafcbf4a 100644
--- a/dist/constants.cjs
+++ b/dist/constants.cjs
@@ -22,6 +22,7 @@ exports.SUPPORTED_NETWORKS_ACCOUNTS_API_V4 = [
     '0x8f', // 143
     '0x3e7', // 999 HyperEVM
     '0x13b2', // 5042 Arc
+    '0x1237', // 4663 Robinhood
 ];
 /** Lowercase ERC-20 address for MetaMask USD (mUSD), same contract on listed chains. */
 exports.MUSD_ERC20_ADDRESS_LOWER = '0xaca92e438df0b2401ff60da7e4337b687a2435da';
diff --git a/dist/constants.mjs b/dist/constants.mjs
index 2be6f4b8339b1102e9ed3c4685f4935293d6fa27..7e3b1d69b826f2cc35aa803936b59bee0c8d7a80 100644
--- a/dist/constants.mjs
+++ b/dist/constants.mjs
@@ -19,6 +19,7 @@ export const SUPPORTED_NETWORKS_ACCOUNTS_API_V4 = [
     '0x8f', // 143
     '0x3e7', // 999 HyperEVM
     '0x13b2', // 5042 Arc
+    '0x1237', // 4663 Robinhood
 ];
 /** Lowercase ERC-20 address for MetaMask USD (mUSD), same contract on listed chains. */
 export const MUSD_ERC20_ADDRESS_LOWER = '0xaca92e438df0b2401ff60da7e4337b687a2435da';
