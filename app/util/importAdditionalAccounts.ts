const MAX = 20;

/**
 * Add additional accounts in the wallet based on balance
 */
// eslint-disable-next-line arrow-body-style
export default async (_maxAccounts: number = MAX, _index: number = 0) => {
  return;
};
