// * Constants

// * Selectors

// * Reducer
export const initialState = {};

function swapsReducer(state = initialState, action) {
	switch (action.type) {
		default: {
			return state;
		}
	}
}

export default swapsReducer;
