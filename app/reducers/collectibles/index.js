const initialState = {
	favorites: {},
};

const fiatOrderReducer = (state = initialState, action) => {
	switch (action.type) {
		default: {
			return state;
		}
	}
};

export default fiatOrderReducer;
