import { StyleSheet } from 'react-native';

/**
 * StyleSheet for the SampleCounterPane component
 * 
 * Defines the layout and styling for the counter card and its button.
 * 
 * @returns {StyleSheet} A StyleSheet object containing the following styles:
 * - card: Styles for the main card container
 * - button: Styles for the increment button
 * 
 * @sampleFeature do not use in production code
 */
const styleSheet = () => StyleSheet.create({
    card : {
        width: '100%',
    },
    button: {
        width: '100%',
    }
});

export default styleSheet;
