// TODO(TO-796): Emit from QrSync/MWP connection failure path so add-device
// scanner recovery UI is reachable at runtime (currently test-only).
export const EXTENSION_ACCOUNT_SYNC_CONNECTION_FAILED_EVENT =
  'extensionAccountSyncConnectionFailed';
