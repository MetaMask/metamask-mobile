import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    heading: {
      marginTop: 16,
    },
    desc: {
      marginTop: 8,
    },
    accessory: {
      marginTop: 16,
    },
  });

export default styleSheet;
