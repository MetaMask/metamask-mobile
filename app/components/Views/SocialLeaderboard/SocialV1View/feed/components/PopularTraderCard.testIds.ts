export const getPopularTraderCardTestId = (traderId: string): string =>
  `popular-trader-card-${traderId}`;

export const getPopularTraderCardFollowTestId = (traderId: string): string =>
  `popular-trader-card-follow-${traderId}`;

export const getPopularTraderCardAvatarTestId = (traderId: string): string =>
  `popular-trader-card-avatar-${traderId}`;
