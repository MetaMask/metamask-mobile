import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    textContent: { paddingHorizontal: 16 },
    footerContainer: {
      paddingTop: 24,
      paddingBottom: 16,
      paddingHorizontal: 8,
    },
  });

export default createStyles;
