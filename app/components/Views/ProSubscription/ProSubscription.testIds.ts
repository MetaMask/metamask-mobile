export const ProSubscriptionTestIds = {
  BACK_BUTTON: 'pro-subscription-back-button',
  /** @deprecated the view now pushes; kept so the existing spec still resolves. */
  CLOSE_BUTTON: 'pro-subscription-close-button',
} as const;
