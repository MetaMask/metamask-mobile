import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    optionButton: {
      alignItems: 'center',
      flexDirection: 'row',
      padding: 12,
      gap: 16,
    },
  });

export default styleSheet;
