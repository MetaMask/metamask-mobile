import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    noNetworkFeeContainer: {
      alignSelf: 'center',
      height: 22,
    },
  });

export default createStyles;
