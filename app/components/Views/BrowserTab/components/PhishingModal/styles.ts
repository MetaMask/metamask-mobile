import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    fullScreenModal: {
      flex: 1,
      margin: 0,
      justifyContent: 'center',
      alignItems: 'center',
    },
  });

export default styleSheet;
