import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    root: {
      flex: 1,
      paddingHorizontal: 16,
      paddingTop: 16,
    },
    backButton: {
      padding: 10,
    },
  });

export default styleSheet;
