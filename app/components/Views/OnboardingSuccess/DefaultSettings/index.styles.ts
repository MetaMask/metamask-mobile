import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    root: {
      flex: 1,
      paddingHorizontal: 16,
      paddingTop: 16,
    },
  });

export default styleSheet;
