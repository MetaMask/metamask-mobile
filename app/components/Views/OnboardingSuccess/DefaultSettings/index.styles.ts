import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    root: {
      flex: 1,
      paddingTop: 16,
    },
    textContainer: {
      paddingHorizontal: 16,
    },
  });

export default styleSheet;
