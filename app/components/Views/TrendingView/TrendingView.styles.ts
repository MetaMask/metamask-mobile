import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    scrollView: {
      flex: 1,
      marginTop: 48,
      paddingLeft: 16,
      paddingRight: 16,
    },
  });

export default styleSheet;
