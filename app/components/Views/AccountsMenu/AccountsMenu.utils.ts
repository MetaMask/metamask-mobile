// Re-exported so `AccountsMenu` (and the tests that mock this module to flip
// the beta branch) keep importing it from here.
export { getBetaSupportUrl } from '../../../util/support/betaSupportUrl';
