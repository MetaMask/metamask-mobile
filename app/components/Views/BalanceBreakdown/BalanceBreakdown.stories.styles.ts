import { StyleSheet } from 'react-native';

export const balanceBreakdownStoryStyles = StyleSheet.create({
  paddedCentered: {
    padding: 16,
    alignItems: 'center',
  },
  padded: {
    padding: 16,
  },
});
