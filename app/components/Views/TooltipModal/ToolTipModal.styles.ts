import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    content: {
      paddingHorizontal: 16,
    },
    footerContainer: {
      paddingHorizontal: 16,
      paddingTop: 24,
      paddingBottom: 16,
    },
  });

export default styleSheet;
