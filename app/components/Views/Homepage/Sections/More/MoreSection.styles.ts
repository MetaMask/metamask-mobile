import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sectionGap: { gap: 8 },
  row: {
    alignItems: 'center',
    flexDirection: 'row',
    minHeight: 48,
    paddingVertical: 8,
  },
  startIcon: {
    marginRight: 16,
  },
  label: {
    flex: 1,
  },
});

export default styles;
