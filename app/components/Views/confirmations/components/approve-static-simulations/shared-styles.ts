import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    amountAndAddressContainer: {
      flexDirection: 'row',
      gap: 4,
    },
  });

export default styleSheet;
