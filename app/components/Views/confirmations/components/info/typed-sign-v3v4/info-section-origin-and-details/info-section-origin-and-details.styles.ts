import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    dividerContainer: {
      marginTop: 4,
      marginBottom: 12,
    },
  });

export default styleSheet;
