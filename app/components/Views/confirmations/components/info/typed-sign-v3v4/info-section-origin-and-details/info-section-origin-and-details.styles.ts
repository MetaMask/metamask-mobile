import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    dividerContainer: {
      marginTop: 4,
      marginBottom: 12,
    },
    networkRowContainer: {
      flexDirection: 'row',
      alignItems: 'center',
    },
    avatarNetwork: {
      marginRight: 4,
    },
  });

export default styleSheet;
