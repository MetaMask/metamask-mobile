import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    estAnnualReward: {
      flexDirection: 'row',
      gap: 8,
    },
    aprTooltipContentContainer: {
      gap: 8,
    },
  });

export default styleSheet;
