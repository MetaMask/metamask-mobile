import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    button: {
      width: '100%',
    },
    inputsContainer: {
      marginTop: 16,
      marginBottom: 8,
    },
  });

export default styleSheet;
