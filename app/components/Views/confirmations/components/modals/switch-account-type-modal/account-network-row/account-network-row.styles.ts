import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    multichain_accounts_row_wrapper: {
      width: '100%',
      marginBottom: 8,
    },
  });

export default styleSheet;
