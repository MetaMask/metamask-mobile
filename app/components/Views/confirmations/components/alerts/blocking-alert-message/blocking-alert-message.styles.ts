import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      paddingTop: 32,
    },
    message: {
      textAlign: 'center',
    },
  });

export default styleSheet;
