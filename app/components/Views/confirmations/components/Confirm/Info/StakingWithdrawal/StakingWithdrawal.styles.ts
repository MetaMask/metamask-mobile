import { StyleSheet } from 'react-native';

const styleSheet = () => StyleSheet.create({
    stakingContractInteractionDetailsContainer: {
        paddingVertical: 8,
    },
  });

export default styleSheet;
