import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    networkContainer: {
      display: 'flex',
      flexDirection: 'row',
      alignItems: 'center',
    },
  });

export default styleSheet;
