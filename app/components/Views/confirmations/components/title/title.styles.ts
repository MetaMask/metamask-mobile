import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    titleContainer: {
      marginVertical: 24,
      paddingHorizontal: 16,
    },
    title: {
      textAlign: 'center',
    },
    subTitle: {
      marginTop: 8,
      textAlign: 'center',
    },
  });

export default styleSheet;
