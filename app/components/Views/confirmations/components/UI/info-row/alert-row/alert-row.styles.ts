import { StyleSheet } from 'react-native';

const styleSheet = () => {

  return StyleSheet.create({
    infoRowOverride: {
      paddingBottom: 4,
      paddingHorizontal: 8,
    },
  });
};

export default styleSheet;
