import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    iconContainer: {
      marginLeft: 4,
      marginTop: 1, // Slight adjustment for visual centering with text
    },
  });

export default styleSheet;
