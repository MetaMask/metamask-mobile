import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    error: {
      marginTop: 4,
    },
    label: {
      marginBottom: 6,
    },
  });

export default styleSheet;
