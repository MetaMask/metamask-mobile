import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    skeleton: {
      marginBottom: 8,
      borderRadius: 8,
    },
    switchSkeleton: {
      marginBottom: 8,
      borderRadius: 8,
    },
  });

export default createStyles;
