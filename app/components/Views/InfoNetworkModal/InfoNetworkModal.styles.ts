import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    bottomModal: {
      justifyContent: 'flex-end',
      margin: 0,
    },
  });

export default createStyles;
