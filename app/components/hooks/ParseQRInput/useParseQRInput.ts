import { useCallback } from 'react';

export enum QRError {
  ERROR_ADDRESS_INPUT = 'error_address_input',
}

const useParseQRInput = () => {
  const isValidSendInput
  return { error };
};

export default useParseQRInput;
