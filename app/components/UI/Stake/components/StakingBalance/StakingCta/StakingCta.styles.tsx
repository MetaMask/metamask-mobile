import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    contentMain: {
      flexDirection: 'row',
      flexWrap: 'wrap',
      borderRadius: 12,
      marginBottom: 8,
    },
  });

export default styleSheet;
