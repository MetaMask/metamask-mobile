import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      maxHeight: 552,
      paddingHorizontal: 16,
      gap: 12,
    },
    listItemContainer: {
      padding: 16,
    },
  });

export default styleSheet;
