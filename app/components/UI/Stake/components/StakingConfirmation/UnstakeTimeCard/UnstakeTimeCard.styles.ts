import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    card: {
      borderWidth: 0,
      borderRadius: 8,
      gap: 16,
    },
  });

export default styleSheet;
