import { StyleSheet } from 'react-native';

const stylesSheet = () =>
  StyleSheet.create({
    card: {
      borderWidth: 0,
      gap: 16,
      borderRadius: 8,
    },
    estAnnualRewardValue: {
      flexDirection: 'row',
      gap: 8,
    },
  });

export default stylesSheet;
