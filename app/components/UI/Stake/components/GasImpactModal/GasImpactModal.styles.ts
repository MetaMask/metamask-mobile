import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      paddingHorizontal: 16,
    },
    contentBody: {
      paddingBottom: 16,
    },
    footer: {
      paddingBottom: 16,
    },
  });

export default styleSheet;
