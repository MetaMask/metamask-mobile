import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    chartContainer: {
      paddingVertical: 16,
    },
    chart: {
      height: 112,
    },
  });

export default styleSheet;
