import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      paddingVertical: 16,
      gap: 4,
      alignItems: 'center',
    },
  });

export default styleSheet;
