import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    safeArea: {
      flex: 1,
    },
    scrollContent: {
      paddingBottom: 100,
    },
  });

export default styleSheet;
