import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    cardImage: {
      width: 150,
      height: 95,
      borderRadius: 5,
    },
  });

export default styleSheet;
