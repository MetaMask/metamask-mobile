import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      height: 82,
      borderRadius: 12,
      paddingHorizontal: 16,
    },
  });

export default styleSheet;
