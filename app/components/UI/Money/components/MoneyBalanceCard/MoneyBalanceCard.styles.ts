import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      minHeight: 82,
      borderRadius: 12,
      paddingHorizontal: 16,
      paddingVertical: 16,
      marginHorizontal: 16,
    },
  });

export default styleSheet;
