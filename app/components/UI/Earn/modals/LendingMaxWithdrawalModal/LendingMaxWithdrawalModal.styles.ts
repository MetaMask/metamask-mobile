import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    bodyTextContainer: {
      gap: 16,
      paddingHorizontal: 16,
      paddingBottom: 16,
    },
  });

export default styleSheet;
