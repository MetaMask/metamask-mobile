import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    networkAvatar: {
      height: 32,
      width: 32,
      flexGrow: 1,
    },
  });

export default styleSheet;
