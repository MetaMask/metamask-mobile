import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    flatList: {
      maxHeight: 552,
      paddingHorizontal: 16,
      gap: 12,
    },
    listItemContainer: {
      paddingVertical: 16,
    },
  });

export default styleSheet;
