// Matches all characters that aren't integers or period (".")
export const NON_NUMERIC_EXCEPT_DECIMAL_REGEX = /[^0-9.]/g;

// Matches all periods (".")
export const ALL_DECIMAL_POINTS_REGEX = /\./g;
