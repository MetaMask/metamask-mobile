/** Rive / Image fill the hero `Box` (`h-52`); defined once to avoid per-render style objects. */
export const WALLET_HOME_ONBOARDING_HERO_MEDIA_LAYOUT_STYLE = {
  width: '100%' as const,
  height: '100%' as const,
  minHeight: 0,
};
