diff a/app/components/UI/PaymentRequest/index.js b/app/components/UI/PaymentRequest/index.js	(rejected hunks)
@@ -392 +392,7 @@ class PaymentRequest extends Component {
-			res = handleFiatPrimaryCurrency(amount, conversionRate, currentCurrency, contractExchangeRates, selectedAsset);
+			res = handleFiatPrimaryCurrency(
+				amount,
+				conversionRate,
+				currentCurrency,
+				contractExchangeRates,
+				selectedAsset
+			);
@@ -394 +400,7 @@ class PaymentRequest extends Component {
-			res = handleETHPrimaryCurrency(amount, conversionRate, currentCurrency, contractExchangeRates, selectedAsset);
+			res = handleETHPrimaryCurrency(
+				amount,
+				conversionRate,
+				currentCurrency,
+				contractExchangeRates,
+				selectedAsset
+			);
@@ -397 +409 @@ class PaymentRequest extends Component {
-		this.setState({ amount, cryptoAmount, secondaryAmount, secondaryCurrency,  symbol, showError: false });
+		this.setState({ amount, cryptoAmount, secondaryAmount, secondaryCurrency, symbol, showError: false });
@@ -450 +462,9 @@ class PaymentRequest extends Component {
-		const { amount, secondaryAmount, secondaryCurrency, symbol, cryptoAmount, showError, selectedAsset } = this.state;
+		const {
+			amount,
+			secondaryAmount,
+			secondaryCurrency,
+			symbol,
+			cryptoAmount,
+			showError,
+			selectedAsset
+		} = this.state;
@@ -483 +503 @@ class PaymentRequest extends Component {
-								</View>	
+								</View>
