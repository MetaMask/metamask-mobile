import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    address: {
      flexDirection: 'row',
      alignItems: 'center',
    },
    copyButton: {
      padding: 4,
    },
  });

export default styleSheet;
