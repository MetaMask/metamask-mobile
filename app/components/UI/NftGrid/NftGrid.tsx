import { Text, View } from 'react-native';
import React from 'react';

const NftGrid = () => {
  const dd = 'NftGrid';
  return (
    <View>
      <Text>{dd}</Text>
    </View>
  );
};

export default NftGrid;
