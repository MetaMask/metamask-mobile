import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    container: {
      flexDirection: 'row',
      gap: 12,
    },
    buttonWrapper: {
      flex: 1,
    },
  });

export default styleSheet;
