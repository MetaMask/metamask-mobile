import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    content: {
      paddingHorizontal: 16,
      paddingVertical: 8,
    },
  });

export default styleSheet;
