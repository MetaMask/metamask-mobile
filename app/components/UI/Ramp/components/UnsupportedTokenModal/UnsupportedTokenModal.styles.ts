import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    content: {
      padding: 24,
      gap: 16,
    },
    footer: {
      padding: 24,
      paddingTop: 0,
    },
  });

export default styleSheet;
