import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    region: {
      flexDirection: 'row',
      alignItems: 'center',
    },
    emoji: {
      marginRight: 12,
    },
  });

export default styleSheet;
