import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    state: {
      flexDirection: 'row',
      alignItems: 'center',
    },
  });

export default styleSheet;
