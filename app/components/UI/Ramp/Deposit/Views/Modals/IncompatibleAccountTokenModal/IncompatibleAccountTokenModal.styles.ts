import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    headerTitle: {
      textAlign: 'center',
    },
    content: {
      padding: 24,
    },
  });

export default styleSheet;
