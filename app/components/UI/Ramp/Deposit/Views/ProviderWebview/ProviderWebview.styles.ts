import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    description: {
      textAlign: 'center',
      marginTop: 16,
    },
    loadingContainer: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
  });

export default styleSheet;
