import { StyleSheet } from 'react-native';

const styleSheet = () => {
  return StyleSheet.create({
    description: {
      textAlign: 'center',
      marginTop: 16,
    },
  });
};

export default styleSheet;
