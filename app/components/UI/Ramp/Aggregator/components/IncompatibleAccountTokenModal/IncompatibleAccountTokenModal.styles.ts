import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    headerTitle: {
      textAlign: 'center',
    },
    content: {
      paddingHorizontal: 16,
      gap: 16,
    },
  });

export default styleSheet;
