import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flexGrow: 1, justifyContent: 'center' },
  getStartedImageWrapper: { alignItems: 'center' },
});

export default styles;
