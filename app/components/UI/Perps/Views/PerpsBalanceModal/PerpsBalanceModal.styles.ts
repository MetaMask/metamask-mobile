import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    content: {
      padding: 24,
    },
    actionButton: {
      marginBottom: 12,
    },
  });

export default createStyles;
