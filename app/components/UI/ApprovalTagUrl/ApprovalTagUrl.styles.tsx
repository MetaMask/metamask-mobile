// Third party dependencies.
import { StyleSheet } from 'react-native';

/**
 * Style sheet function for ApprovalTagUrl component.
 * @returns StyleSheet object.
 */
const styleSheet = () =>
  StyleSheet.create({
    tagUrl: {
      marginBottom: 16,
    },
  });

export default styleSheet;
