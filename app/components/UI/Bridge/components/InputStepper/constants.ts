export enum InputStepperDescriptionType {
  WARNING = 'warning',
  ERROR = 'error',
}

/**
 * Room kept on top of the measured text width so the caret sitting after the
 * last character is not clipped.
 */
export const INPUT_CARET_WIDTH = 3;
