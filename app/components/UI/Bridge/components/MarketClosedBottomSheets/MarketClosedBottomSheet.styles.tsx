import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    container: {
      paddingHorizontal: 16,
    },
    footer: {
      paddingVertical: 20,
      paddingHorizontal: 16,
    },
  });

export default createStyles;
