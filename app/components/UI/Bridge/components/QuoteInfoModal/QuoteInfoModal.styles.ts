import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    container: {
      paddingHorizontal: 16,
      gap: 16,
    },
    footer: {
      paddingVertical: 0,
      paddingHorizontal: 0,
      paddingBottom: 16,
    },
  });

export default createStyles;
