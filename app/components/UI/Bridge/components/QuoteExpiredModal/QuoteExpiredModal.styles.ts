import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    container: {
      paddingHorizontal: 16,
    },
    footer: {
      paddingHorizontal: 16,
      paddingTop: 24,
      paddingBottom: 16,
    },
  });

export default createStyles;
