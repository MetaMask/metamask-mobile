import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    ethLogo: {
      width: 40,
      height: 40,
      borderRadius: 20,
      overflow: 'hidden',
    },
  });

export default styleSheet;
