/* eslint-disable import/prefer-default-export */

// External dependencies.
import { SkeletonShape } from './Skeleton.types';

// Defaults
export const DEFAULT_SKELETON_SHAPE = SkeletonShape.Rectangle;
