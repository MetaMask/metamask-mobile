// Third party dependencies.
import React from 'react';
import Icon from '../../../../../../Icon';

// Internal dependencies.
import { AvatarAssetIconProps } from './AvatarAssetIcon.types';

const AvatarAssetIcon = ({ ...props }: AvatarAssetIconProps) => (
  <Icon {...props} />
);

export default AvatarAssetIcon;
