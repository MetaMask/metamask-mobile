import { BadgeCustomPosition } from './Badge.types';

// eslint-disable-next-line import/prefer-default-export
export const DEFAULT_BADGE_POSITION: BadgeCustomPosition = {
  top: 'auto',
  right: 'auto',
  bottom: 'auto',
  left: 'auto',
};

export const BADGE_CONTENT_TEST_ID = 'badge-content';
