// Internal dependencies.
import { BadgeNetworkProps } from './variants/BadgeNetwork/BadgeNetwork.types';

/**
 * Badge variants.
 */
export enum BadgeVariants {
  Network = 'network',
}

/**
 * Badge Account component props.
 */
export type BadgeProps = BadgeNetworkProps;
