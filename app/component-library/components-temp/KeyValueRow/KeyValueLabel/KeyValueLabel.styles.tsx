import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    labelContainer: {
      flexDirection: 'row',
      alignItems: 'center',
    },
  });

export default styleSheet;
