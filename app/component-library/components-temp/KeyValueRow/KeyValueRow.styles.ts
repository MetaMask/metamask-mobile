import { StyleSheet } from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    flexRow: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 8,
    },
  });

export default styleSheet;
