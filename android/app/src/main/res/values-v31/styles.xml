<resources>

    <!-- AppTheme for API 31+ (Android 12+): adds system splash screen attributes so the
         OS splash and the expo-splash-screen overlay show the same fox on the same background,
         making the transition seamless. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@drawable/app_background</item>
        <item name="android:statusBarColor">@color/theme</item>
        <item name="android:navigationBarColor">@color/navBar</item>
        <item name="android:windowLightStatusBar">@bool/useLightStatusBar</item>
        <item name="android:textColor">@color/themeInverse</item>
        <!-- Android 12+ splash screen.
             splash_icon centers the fox at natural size so it fits inside the circular
             mask without clipping. Icon background matches the outer background so the
             circle boundary is invisible — result looks identical to expo's overlay. -->
        <item name="android:windowSplashScreenBackground">@color/theme</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/theme</item>
    </style>

</resources>
