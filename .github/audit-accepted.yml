# Advisories the audit owner has consciously accepted as risk (e.g. a
# dev-only tool, an unreachable code path, or a fix that is not yet
# published). Listed advisory IDs are permanently skipped by
# dependency-audit-escalation.yml's fix/PR/issue loop until removed here.
#
# See docs/readme/dependency-audit.md for the acceptance process. Every entry
# must include a reason and the GitHub handle who accepted it, and should be
# revisited periodically — this file is not a way to silence an advisory
# forever without review.
#
# accepted:
#   - id: 'GHSA-xxxx-xxxx-xxxx'
#     reason: 'Only reachable from a dev-only script; not shipped to users.'
#     accepted_by: 'gh-handle'
#     accepted_on: '2026-08-31'

accepted: []
