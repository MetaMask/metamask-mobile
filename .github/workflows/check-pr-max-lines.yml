name: "Check PR Max Lines"

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  check-pr-max-lines:
    uses: metamask/github-tools/.github/workflows/pr-line-check.yml@ci-pr-line-check
    with:
      max_lines: 1000
      ignore_patterns: '(\.lock|\.snap)$'
