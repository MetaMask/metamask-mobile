# Appium Smoke Tests — iOS
#
# Fans out one reusable job per Appium smoke tag. Add a matching job below.
# Invoked from ci.yml on main push/schedule when ios-tests-ready succeeds.
# iOS build artifacts come from build-ios-apps in the parent ci.yml workflow.

name: Appium Smoke Tests (iOS)

on:
  workflow_call:
    inputs:
      build_type:
        description: 'Build type (main or flask)'
        required: false
        type: string
        default: 'main'
      metamask_environment:
        description: 'MetaMask environment'
        required: false
        type: string
        default: 'e2e'
      runner_provider:
        description: 'Runner provider (namespace or current)'
        required: false
        type: string
        default: 'current'
      selected_tags:
        description: 'JSON array of tags from smart E2E selector (e.g. ["SmokeAccounts"] or ["ALL"])'
        required: false
        type: string
        default: '["ALL"]'

permissions:
  contents: read
  checks: write

jobs:
  appium-accounts-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeAccounts'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-accounts-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeAccounts
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-perps-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokePerps'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-perps-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokePerps
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-predictions-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokePredictions'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-predictions-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokePredictions
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  # Thin seedless keep list — 2 shards (aligned with main).
  appium-seedless-onboarding-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeSeedlessOnboarding'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-seedless-onboarding-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeSeedlessOnboarding
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit


  appium-snaps-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeSnaps'))
      }}
    strategy:
      matrix:
        split: [1, 2, 3, 4, 5, 6]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-snaps-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeSnaps
      split_number: ${{ matrix.split }}
      total_splits: 6
      # Session reuse + denser shards; headroom vs former 25m single-shard load.
      test-timeout-minutes: 30
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-network-abstractions-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeNetworkAbstractions'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-network-abstractions-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeNetworkAbstractions
      split_number: ${{ matrix.split }}
      total_splits: 2
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-wallet-platform-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeWalletPlatform'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-wallet-platform-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeWalletPlatform
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-swap-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeSwap'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-swap-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeSwap
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-stake-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeStake'))
      }}
    strategy:
      matrix:
        split: [1]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-stake-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeStake
      split_number: ${{ matrix.split }}
      total_splits: 1
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-money-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeMoney'))
      }}
    strategy:
      matrix:
        split: [1]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-money-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeMoney
      split_number: ${{ matrix.split }}
      total_splits: 1
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-browser-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeBrowser'))
      }}
    strategy:
      matrix:
        split: [1]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-browser-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeBrowser
      split_number: ${{ matrix.split }}
      total_splits: 1
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-confirmations-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeConfirmations'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-confirmations-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeConfirmations
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 35
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-multichain-api-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeMultiChainAPI'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-multichain-api-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeMultiChainAPI
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit

  appium-network-expansion-ios-smoke:
    if: >-
      ${{
        !cancelled() &&
        (contains(fromJson(inputs.selected_tags || '["ALL"]'), 'ALL') ||
         contains(fromJson(inputs.selected_tags || '["ALL"]'), 'SmokeNetworkExpansion'))
      }}
    strategy:
      matrix:
        split: [1, 2]
      fail-fast: false
    uses: ./.github/workflows/run-appium-e2e-workflow.yml
    with:
      test-suite-name: appium-network-expansion-ios-smoke-${{ matrix.split }}
      platform: ios
      test_suite_tag: SmokeNetworkExpansion
      split_number: ${{ matrix.split }}
      total_splits: 2
      test-timeout-minutes: 25
      build_type: ${{ inputs.build_type || 'main' }}
      metamask_environment: ${{ inputs.metamask_environment || 'e2e' }}
      runner_provider: ${{ inputs.runner_provider || 'current' }}
    secrets: inherit
