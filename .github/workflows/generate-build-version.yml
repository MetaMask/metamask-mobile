##############################################################################################
#
# Generate Build Version (reusable)
#
# Obtains the next build number from MetaMask/metamask-mobile-build-version via OIDC.
#
##############################################################################################
name: Generate Build Version

on:
  workflow_call:
    outputs:
      build-version:
        description: 'Generated build version number'
        value: ${{ jobs.generate.outputs.build-version }}

permissions:
  id-token: write

jobs:
  generate:
    uses: MetaMask/metamask-mobile-build-version/.github/workflows/metamask-mobile-build-version.yml@449f5946391e64282d6d6a5cee228c6add9b6acb # v0.3.0
    permissions:
      id-token: write
