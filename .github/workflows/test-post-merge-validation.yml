name: Test Merge Validation

on:
  push:
    branches:
      - chore-post-validation

permissions:
  pull-requests: write
  issues: write
  contents: read

jobs:
  test-validation:
    uses: MetaMask/github-tools/.github/workflows/post-merge-validation.yml@chore-post-validation
    with:
      repo-owner: MetaMask
      repo-name: metamask-mobile
      pr-number: '17911'
      pr-author: 'christopherferreira9'
      pr-title: 'fix: cp-7.53.0 disables flaky E2E tests'