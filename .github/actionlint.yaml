self-hosted-runner:
  # Labels of self-hosted runner in array of strings.
  labels: 
    - "gha-mmsdk-scale-set-ubuntu-22.04-amd64-xl"
    - "gha-mmsdk-scale-set-ubuntu-22.04-amd64-xxl"
    - "gha-mmsdk-scale-set-ubuntu-22.04-amd64-l-kvm"
    - "gha-mmsdk-scale-set-ubuntu-22.04-amd64-large"
    - "gha-mm-scale-set-ubuntu-22.04-amd64-large"
    - "gha-mm-scale-set-ubuntu-22.04-amd64-small"
    - "gha-mm-scale-set-ubuntu-22.04-amd64-med"
    - "macos-15"
    - "ghcr.io/cirruslabs/macos-runner:tahoe"
    - "ghcr.io/cirruslabs/macos-runner:tahoe-xl"
    - "ghcr.io/cirruslabs/ubuntu-runner-amd64:24.04-md"
    - "ghcr.io/cirruslabs/ubuntu-runner-amd64:24.04-lg"
    - "ghcr.io/cirruslabs/ubuntu-runner-amd64:24.04-xl"
    - "low-priority"
    - "metamask-ci-linux"
    # Placeholder — replace with the canonical Namespace Linux label before dispatching
    # with runner_provider: namespace. See .phase0/namespace-artifacts.md (INFRA-3592).
    - "nscloud-PLACEHOLDER-CONFIRM-LABEL"
    - "nscloud-ubuntu-22.04-amd64-2x4"
    - "nscloud-ubuntu-22.04-amd64-4x8"
    - "nscloud-ubuntu-22.04-amd64-8x16"
    - "nscloud-ubuntu-22.04-amd64-16x32"
    - "nscloud-ubuntu-22.04-amd64-32x64"
    - "nscloud-ubuntu-24.04-amd64-2x4"
    - "nscloud-ubuntu-24.04-amd64-4x8"
    - "nscloud-ubuntu-24.04-amd64-8x16"
    - "nscloud-ubuntu-24.04-amd64-16x32"
    - "nscloud-ubuntu-24.04-amd64-32x64"

# Configuration variables in array of strings defined in your repository or
# organization. `null` means disabling configuration variables check.
# Empty array means no configuration variable is allowed.
config-variables: null

# Configuration for file paths. The keys are glob patterns to match to file
# paths relative to the repository root. The values are the configurations for
# the file paths. Note that the path separator is always '/'.
# The following configurations are available.
#
# "ignore" is an array of regular expression patterns. Matched error messages
# are ignored. This is similar to the "-ignore" command line option.
paths:
