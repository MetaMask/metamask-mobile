# Windows files should use crlf line endings
# https://help.github.com/articles/dealing-with-line-endings/
*.bat text eol=crlf

# Shell scripts must keep LF so they run in WSL on Windows.
*.sh text eol=lf

# Yarn patches may contain required context from generated upstream artifacts.
.yarn/patches/@metamask-perps-controller-npm-13.0.0-438f476ec4.patch whitespace=-trailing-space
