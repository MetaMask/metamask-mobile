/**
 * @deprecated Prefer importing helpers from `./package` or
 * `./MockSeedlessOnboardingController`. Kept so older relative redirects still
 * resolve during Metro transition.
 */
export {
  MockSeedlessOnboardingController,
  E2EMockSeedlessHelpers,
} from './MockSeedlessOnboardingController';

export { E2EMockSeedlessHelpers as default } from './MockSeedlessOnboardingController';
