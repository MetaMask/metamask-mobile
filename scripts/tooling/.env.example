# Copy this file to `.env` and fill in the values your gateway admin gave you.
# `.env` is gitignored — never commit real credentials.

PUSHGATEWAY_URL=https://pushgateway-mmcx-devlake-dev.o11y.web3factory.consensys.net
PUSHGATEWAY_USERNAME=
PUSHGATEWAY_PASSWORD=
