{
  "name": "kalshi-poc-backend",
  "version": "0.0.1",
  "private": true,
  "description": "Throwaway stub backend for the Kalshi Predict POC. Holds the ISV admin api_key, mints + stores per-user PEMs, signs RSA-PSS Kalshi requests, exposes canonical /predict/v1/kalshi/* endpoints.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/node": "^20.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.4.0"
  }
}
