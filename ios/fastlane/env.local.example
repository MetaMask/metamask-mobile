# App Store Connect API Key Configuration
# Copy this file to .env.local and fill in your values
# 
# IMPORTANT: .env.local is gitignored - never commit your actual API keys!
#
# Usage:
#   1. Copy this file: cp env.local.example .env.local
#   2. Edit .env.local with your actual values
#   3. Source it: source .env.local
#   4. Run Fastlane: bundle exec fastlane list_testflight_groups

# Your App Store Connect Issuer ID
# Found in App Store Connect → Users and Access → Keys
export APP_STORE_CONNECT_API_KEY_ISSUER_ID="your-issuer-id-here"

# Your App Store Connect API Key ID
# Found in App Store Connect → Users and Access → Keys
export APP_STORE_CONNECT_API_KEY_KEY_ID="your-key-id-here"

# Path to your downloaded .p8 key file
# Download from App Store Connect → Users and Access → Keys
export APP_STORE_CONNECT_API_KEY_KEY_FILEPATH="/path/to/AuthKey_XXXXXXXXXX.p8"

